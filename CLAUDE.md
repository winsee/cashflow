# 现金流游戏辅助工具（Cashflow Companion）

把纸质《富爸爸现金流》桌游做成线上辅助工具：实体棋盘/骰子/卡牌照常线下玩，每人手机开网页（PWA）完成卡牌识别、自动记账、全员状态同步。

## 当前状态（2026-08-08）

**代码 M0–M4 已完成并跑通**（约 8000 行：房间/回合/记账/快车道/破产/OCR/部署）。

**v3 全量卡库升级已完成。** 引擎已兼容 194 张实录卡（v3 schema）：三种计价基准、5 个新
subtype（骰子赌局/收藏品/溢价收购/现金流调整/分期收款）、`chargeOnce`/`appliesTo`/
`buyerScope` 等细节、记录卡四栏收入分类均已落地。分期收款（mk-029）按房主裁决实现为
「冻结房产」模型（不移房/不解押，满 $100,000 才移交，破产出局直接清零，见 design/06 §6.4）。
测试 **391 passed / 1 skipped**（原 108 条 + §5 的 13 条会算错钱场景 + 194 张逐卡对局扫描
+ 老鼠赛跑→快车道端到端一局）。
`python tools/validate_cards.py` 通过（7 条提示是卡面固有股价越界，正常）。

交接文档：[design/07-v3卡库实现交接.md](design/07-v3卡库实现交接.md)（不兼容清单与实施顺序，已全部完成）。

**浏览器端 OCR 已实现（design/08）。** 服务端 PaddleOCR 在 512MB 云主机上一跑识别就 OOM
（实测 `exit 137`），识别因此搬到手机浏览器（tesseract.js + WASM），服务端只做封闭集匹配
（`POST /api/rooms/{code}/recognize-text`，20~80ms，零 OCR 内存开销）。资源自托管在
`/tesseract/`（构建期由 `web/scripts/sync-tesseract-assets.mjs` 生成，不进 git，不依赖 CDN）。
离线验收：194 张实拍图，四个游戏牌堆严格 Top-3 全部 100%（职业卡 4 张认不出，但它不走扫描）。
降级链：浏览器 OCR → 服务端 PaddleOCR（仅
`--build-arg WITH_OCR=1` 的局域网部署）→ 手动检索。

**还差两项要人在现场做**：真机取景帧命中率（design/08 §6.2）、云端端到端（§6.5，
本地 512MB 容器已验证通过）。服务端 OCR 排障仍用 `/api/health` 与 `/api/health/ocr-probe`。

**邀请二维码 + 系统分享已补齐（FR-1）**：房间页「邀请好友」弹窗给二维码（`uqr` 算矩阵、
自绘内联 SVG，无外部请求）+ 房间码 + 加入链接；分享优先调 `navigator.share` 拉起系统分享面板
（面板里选微信发链接），非安全上下文/微信内自动降级为复制。实现见 `web/src/share.ts`、
`components/{QrCode,InviteDialog}.vue`，设计见 design/03 §7.1。剩真机扫码一次现场确认。

**身份找回与空房清理已补齐（FR-4，design/03 §4.4）**：座位接管现在不限房间状态，等待中的
房间同样能认领座位（昵称撞车时前端按 `NICKNAME_TAKEN` 自动切到座位列表并预选同名座位）；
座位/房间列表带 `online` 标记；无密码 + 未开局 + 无人在线的房间任何人可删，且 1h 后自动清掉。
根因是 `saveSession` 换令牌时没关旧 WebSocket，新会话永远连不上、旧房间广播还会触发
「你已不在该房间」把会话清掉——这条已修并有回归覆盖。

**UX/UI 重构已完成（呈现层重做，功能流程未动）。** 设计稿 45 屏，实现要点：
① **阶段换肤**——老鼠赛跑墨绿、快车道金箔，`<body>` 上挂 `.skin-ft` 覆盖一层颜色变量即整屏换肤，
不逐个组件染色；语义色（正绿负红）与三种卡面不参与换肤。**这个类只在对局页 `/play` 挂**
（2026-08-10 补，见文末）。
② **三种卡面组件**（`components/cards/`）：牌堆卡 `GameCard`（渲染 `raw.{title,body,fields,notes}`，
米白卡纸 + 宋体首行缩进两字 + 顶部牌堆色条）、职业卡 `ProfessionCard`（照实体的黑条分区两栏表）、
快车道格子 `FtSquareCard`（绿格/粉格）。为此 `/api/cards` 增加下发 `raw`。
③ **全员同步看牌**——`ActionTab` 里卡面对所有人渲染，抽卡人得到操作区、其他人看到等待态。
④ **统一弹层** `BaseModal`——「我的待办→页内聚焦卡；别人的动作波及到我→底部弹层」，六个固定槽位。
求购卡按 `card_id` 归组、逐套勾选（服务端本就一套资产一条 prompt，协议未改）。
⑤ **被动回执** `receipts.ts`——从 WS `state.lastEvents`（服务端本就下发）派生，
用 `recentActionAt` 把自己主动做的事排除掉；`flash()` 改成带 variant 的队列。
⑥ 裸 `button` 降级为中性重置，主按钮改显式 `.btn`；抽出 `BaseModal/PageHeader/BaseButton/EmptyState/StatRow`。
⑦ PWA：`vite-plugin-pwa` 预缓存离线壳，`/tesseract/`（13MB）走 CacheFirst 运行时缓存
（详见 `web/vite.config.ts` 里的取舍说明；2026-08-01 复查设计稿对预缓存无要求，房主定案维持现状）；
图标由 `python tools/make_pwa_icons.py` 生成。

**按设计稿收尾（2026-08-01）**：
⑧ **市场风云抽卡人侧**——写清「已通知 N 位持有该资产的玩家 + 谁还没决定」（`state.prompts` 派生，
零协议改动）；市场卡在抽卡瞬间就 `resolved`（`_a_card_drawn`：效果在伴随事件里完成），
所以这一段按牌堆判断而非 `resolved`，卡面上方的状态文案也随之改成「正在等 N 位玩家答复」。
强制卡分支收紧为显式的 `EXPENSE_EVENT/CASH/INSTALLMENT`，新 subtype 不会再静默落进去。
⑨ **波及范围**（`receipts.buildCardImpact` + store `cardImpact`）——现金流调整/强制没收这类
影响多人的卡，逐人列出「谁 · 哪项资产 · 变多少」，全员可见。数据取自事件里引擎算好的
`targets`/`asset_ids`，客户端不重算匹配。（设计稿 §08 画的是「全员支付」的额外支出卡，
但 194 张卡里没有这种卡，故挂到现金流调整卡上，不动卡库与引擎。）
⑩ **日志按轮分组**——`log_rows()` 顺带重放事件流给出每行的 `turn`（撤销行不参与重放），
开局前记 0；前端按 `turn` 分组。靠 `TURN_ENDED` 反推会被停赛/出局玩家带偏，故由服务端给。
⑪ **断线态**——顶部常驻红条「连接断开，正在重连…」（独立槽位，不进 toast 队列）+
内容区「重新连上之前，操作暂不可用」。
⑫ **弹层全部收编**（8/8）：`ConfirmDialog`（`layer="confirm"` 压在上层）、`CardPicker`
（核对弹层出现时选卡层整块收起，不叠层）、大厅两个手写 sheet 均改走 `BaseModal`。
⑬ **大厅文案**——`/api/rooms` 增发 `turnCount`/`currentPlayer`，「继续对局」与房间列表
写出「第 N 轮 · X 人在线 · 轮到谁」。
`npm run ui-smoke` 已扩到 **24 屏**（含选卡/核对不叠层、二次确认、市场抽卡人侧、波及范围、断线态、大厅）。

**试玩反馈的六处收尾（2026-08-01 第二轮）**：
⑭ **回合三步真的分成三步**——银行结算日从「你停在哪种格子？」里拆出来，成为独立的第 ① 步
（设计稿 §03「任何时刻只强调当前该做的那一步」）。内圈 24 格只有 3 个结算日，「本回合没经过」
才是常态，所以第 ① 步给了这个出口：纯前端本地标记（`turnKey` = 轮次+当前玩家），不发事件、
不入账、不改协议，回合一换自动失效；标错了在摘要行点「其实经过了」改回来。
⑮ **HUD 那条进度写出了主语**——「离快车道 · 非工资收入 $X / 总支出 $Y　N%」（快车道是
「距胜利还差 $X」）。设计稿 §03 注解本就定义了它是什么，只是行动页漏了文字，报表/总览都有。
⑯ **非我回合的牌桌做实**——头像圈 + 昵称 + 他走到回合哪一步（`turnPaydayUsed`/`turnSquareUsed`/
`activeCard` 派生）+ 现金与月现金流。原来只有一行「XX 正在行动」，屏幕空荡荡。
⑰ **撤销不再另占一行**（设计稿 §11）——`log_rows()` 反查 `revoked_by` 给出 `revokedBy`
（host/self）与撤销人昵称，日志把 `HOST_REVERTED`/`PLAYER_CORRECTED` 两类审计行过滤掉，
痕迹画在被撤销那一行上（划线 +「已被房主 X 撤销」）。事件流与 `/log` 接口仍然完整。
⑱ **本人更正限当前回合内**（FR-29 语义是「抽错卡当场重选」）——`_revert` 加两道校验：
`current_player_id == actor` 否则 `NOT_YOUR_TURN`；目标 seq 必须大于最后一条未撤销的
`TURN_ENDED` 否则 `TURN_CLOSED`（回合边界只认 `TURN_ENDED`，用 turn_count 反推会被停赛/出局带偏）。
过了这个回合就只能请房主撤销。前端 `correctable()` 同步加 `isMyTurn`。
⑲ **撤销回执只推给当事人**——审计事件补 `target_type`/`target_player_id`/`target_title`，
回执改成设计稿 §10 的「房主撤销了「X」，你的账已重算」并按当事人过滤；`PLAYER_CORRECTED`
的回执整条删掉（本人更正只影响本人，本人那份早被 `SELF_ACTION` 窗口排掉）。
⑳ **弹层只问该问的人**——求购卡服务端本就只推给持有者（`_asset_matches` 逐资产过滤，
已补 6 条回归钉死），弹层补上「这张卡求购**公寓**，你名下有 2 项属于这一类」说清为什么是你，
并过滤掉指向已不在名下资产的残留 prompt；「人人可买」的 4 张股票卡（2BIG×2、CD×2）照旧
对全员弹——每个人都符合条件——但**卖出侧一个字都不给无持仓的人看**（`overSell` 曾在
held=0 时算出「最多可卖 0 股」，已修）。
测试 **443 passed / 1 skipped**；`npm run ui-smoke` 扩到 **30 屏**（`shot()` 改为先等元素再截，
消除日志/报表异步拉数导致的随机失败）。

**规则改动：进入快车道即发放启动资金**（design/02 §9）。金额 = 换算出的现金流量日收入
（非工资收入千元四舍五入 ×100），依 P.5「将获得 100 倍的非工资收入（即您的财产）」，房主定案为进入即到账；
此后每次停在/经过现金流量日再领同额。同时清零老鼠赛跑的 `charity_turns`，
并给 `PAY_OFF_DEBT` 加阶段闸门（记录卡已翻面，快车道不能再清偿）。

**破产已回到说明书的「判定」语义（design/02 §8）**：结算日算出付不出到期款项时，`PAYDAY` 直接产出
`PAYDAY_UNPAYABLE` + `BANKRUPTCY_STARTED` 强制进清算，不再抛 `NEED_LOAN_OR_BANKRUPTCY` 让玩家
「先去贷款」——旧实现把 P.5 的判定写成了二选一，配合无上限贷款就能靠续贷把负现金流永远拖下去。
一并结算多个月时逐月判，付得起的月份照付；`PAYDAY_UNPAYABLE` 只审计不动账，付不出的那个月不结算也不补缴。
贷款本身按说明书不设额度上限（P.4 没有），但破产后不得再贷款。测试 **433 passed / 1 skipped**。

**规则改动：无人认领的梦想可原价买下占位（design/02 §10）**：快车道"你停在哪一个梦想？"弹层里，
以前"没人选"的梦想整个按钮置灰、不可操作；房主复查说明书 P.6"当您停在一个梦想格时，如果您有
足够多的现金，您就可以选择将其买下"发现这是通用表述，并未限定必须有主，遂定案新增第三种操作
`FT_CLAIM_DREAM`：按原价（不叠加）付款、放一块筹码，纯粹是花钱占位——不获胜（胜利仍绑定"自己
选定"的梦想，P.1、design/02 §11 不变）、不加价、不影响任何人的现金流。前端按钮从灰色"没人选"
换成可点的"买下占位"。测试 **445 passed / 1 skipped**。

**逃出老鼠赛跑改成一局里的高光时刻（2026-08-05，纯呈现层，引擎/协议零改动）**：
㉑ **达成条件直接进全屏换算**——不再要求先点 HUD 那条金色横幅。自动弹屏的条件比 `canEscape` 严：
`escapeReady` = 达成 + `isMyTurn` + 手上没待答复的 prompt/未结算的 activeCard。服务端
`_d_enter_fasttrack` 有 `_require_current`，而别人的市场卡完全可能把我的非工资收入顶过线，
所以**非我回合仍旧只给横幅**（点了会被拒的按钮不该自动糊到脸上）。「再想想」记在
`sessionStorage`（key `ftIntro:{room}:{player}`），否则刷新一次就把推开的过场重新弹回来；
收入被打回线下时自动复位。
㉒ **其他玩家：全屏祝贺 + 金色回执**——`FasttrackCheer.vue` 复用 `.curtain` 基座，由
`store.catchCheer()` 从 `ENTERED_FASTTRACK` 事件派生（和 receipts 同一入口、同一份「换快照前」
的旧状态，所以重连首帧 `type:'snapshot'` 不带 `lastEvents`，天然不会误弹）。手上有待答复
弹层的人不弹祝贺屏，只留回执。回执分支用 `player_id === meId` 排除当事人，**不进
`SELF_ACTION` 表**——那张表靠 6 秒时间窗，这里有确定的 id。
㉓ **快车道整屏换成过场屏那条金箔**——`.skin-ft` 的 `--bg-2/--bg-mid/--bg` 与 `.curtain.ftx`
逐字一致（`#FFF6DC → #F3E3B8 → #E8D296`），body 的渐变几何也统一成 `120% 80% at 50% 0%`，
帘幕落下时严丝合缝地接上；`--glow` 归零（底色自带亮心，再叠一层就过曝）。为新增的 `--bg-mid`
在 `:root` 给了个中间值，老鼠赛跑下三段退化成看不出接缝的一条，观感不变。金底深了一档，
`.card.quiet` / `.card.gold` 这两种「靠边框说话」的卡在 `.skin-ft` 下加深了描边。
`npm run ui-smoke` 现 **35 屏**（09 改成断言过场自动弹出，10 验「再想想 + 刷新不重弹」，
12/12b 是其他玩家的祝贺屏与回执存根）。

**进场当回合不再是一排灰按钮（2026-08-05 第二轮，design/02 §9.7）**：
㉔ 试玩反馈「点进快车道后只有现金流量日能点」。根因不是 bug —— `turn_square_used` 是**房间级**
回合标志，`ENTERED_FASTTRACK` 不重置它，而逃出老鼠赛跑的现实路径就是「抽绿卡 → 买资产 → 过线」，
抽卡即置位；改动 ㉑ 让达成条件当场弹过场屏后，大量玩家在买完资产的同一回合进场，这把一直
存在的锁就被推到了脸上。房主定案：**锁是对的**，两种进场方式本就由这个标志天然区分——
自己回合走过一格才进的，本回合到此为止；上一轮被别人的卡顶过线、回合开始就进的，三步照常。
所以引擎语义不动，只做两件事：① `ENTERED_FASTTRACK` 带 `turn_closed`/`turn_count`，
`turn_closed` 为真时把 `turn_payday_used` 一并关上（启动资金刚发过，不关就能再白领一笔），
并把进场轮次记进 `fasttrack.entered_turn`；② 前端 `justLanded`（`entered_turn === turnCount`
且 `turnSquareUsed` 且是我的回合）时整块换成**进场引导卡**，写清「本回合到此为止 · 点结束回合 ·
下回合起掷 2 粒骰」+ 启动资金/现金流量日收入/距胜利还差三个数，过场屏也提前加了第 ⑤ 条预告。
不自动结束回合（房主定案）。`endTurn()` 的二次确认对快车道玩家改说「现金流量日」。
测试 **451 passed / 1 skipped**（新增两条：抽卡当回合进场关回合 + 轮转一圈后恢复、回合开始进场
一切可用；原有 `_give_passive` 直接塞资产绕过 `DRAW_CARD`，这条真实路径此前没被覆盖）；
`npm run ui-smoke` 扩到 **37 屏**（14a 过场屏第 ⑤ 条、14b 进场引导卡）。

**快车道格子排布已补录（2026-08-08，design/05 §3.3）**：`fast_track.json` 此前只有
`businesses`/`dreams` 两个池子、没有排布——模式一按名称选格用不到顺序，但纯线上走棋（design/09）
必须有。从棋盘照片逐格读出 **48 格**并经房主对实物核实，落成 `squares` 数组（`{index, ref}`，
`ref` 指向池子 id 或五个 `ft-s-*` 特殊格；**不存 `type`/逐格 `id`**——前缀与 index 已把它们说尽）。
顺带修正：现金流量日只有 **3** 个（第 12/28/44 格），design/05 v0.1 记的 4 个是错的。
**快车道是一条闭合的单圈跑道**——实体棋盘上它在底边中间向内绕了一段（第 1–14 格贴着中央转盘
走），那是格子太多、长方形盘面排不下的排版手法，不是两条轨道，走法上没有分叉，
所以 design/09 §3.5「快车道摊成同一只轮盘」照旧成立。
`validate_cards.py` 加了 `check_fast_track_board()` 双向对账。

**内圈 24 格也已核实并结构化（2026-08-08，design/05 §1）**：房主对实物确认内圈是**机会格与事件格
严格交替**（奇数格全是机会，末格是市场风云）——`rat_race.json` 里存的一直是对的，而 **design/05 §1
与 design/09 §3.1 抄成了从第 7 格起错开一位的版本**，两处文档已订正、JSON 未动。24 格随之从纯字符串
升级为 `{id, type}`（`rr-01`..`rr-24`，7 种 type），显示名由 type 推出不入库；
`data_loader.py` 加 `RatRaceSquare` 与 `_load_rr_squares()`，`validate_cards.py` 加
`check_rat_race_board()`（格数/id 唯一/type 合法/七种类型计数 12·3·3·3·1·1·1）。
两条轨道的排布至此全部核实完毕。测试 **459 passed / 1 skipped**。

**纯线上模式已落地（2026-08-08，change `add-online-board-mode`）**：一局游戏可以完全在网页里跑完，
不需要任何实体道具。**线下辅助模式一屏、一句文案、一次点击路径都没动**——两套架构靠房间模式分流，
不做「一套界面两种配置」的折中。要点：
㉕ **模式进事件流**——建房时先写一条 `ROOM_MODE_SET`（动作 `SET_ROOM_MODE`），房间一旦有人就
`MODE_LOCKED`。`_revert` 是从**空 `RoomState()`** 重放，模式只存 DB 的话第一次撤销就把纯线上房间打回线下房间。
顺带修了一处既有隐患：`handle_action` 的「大厅最后一人离开即解散房间」只看 `players` 为空，
建房时房主还没 JOIN 会把新房当场删掉，改为同时要求这批事件里有 `PLAYER_LEFT`。
㉖ **随机源全部焊在服务端**（change D10 那张表）：`DRAW_CARD` 带 `cardId`、`PAYDAY`/`FT_PAYDAY`、
`SET_TURN_ORDER`、`PLAYER_CORRECT`、两个识别接口在纯线上一律拒绝，各有明确错误码。
职业卡改为**服务端随机发且不可重抽**（说明书 P.2 步骤 4 写的是「抽」，12 张卡工资差 8 倍，让人自选等于
开局就把平衡拆了）；回合顺序改为**开局服务端替全员掷骰**、平局重摇（P.2「开始游戏」1）；
快车道掷骰格（`FT_BUY_BUSINESS` 的 `diceRule`）也由服务端摇。**梦想仍是玩家手动选**——
说明书步骤 9 用的动词是「选择」，引擎零改动。
㉗ **牌堆 = 事件里的一串 id**（`decks` / `discards`）：`DECKS_SHUFFLED` 把整串牌序写进 payload，
所以「撤销一次发牌 → 牌退回堆顶」不需要写任何退牌逻辑，重放天然等价于「那次发牌从未发生」。
`CARD_DRAWN` 的 apply **按 id 移除而不是 `pop(0)`**——按 id 移除才能让「撤销中间一次发牌后整流重放」成立。
**牌序绝不出房间边界**：`serialize()` 只下发张数，`broadcast_state` 的 `lastEvents` 与 `log_rows()`
两个出口各过一道 `_sanitize_payload()`；SQLite 里的事件流保持完整牌序，**脱敏只发生在出口**。
㉘ **位置是 1-based、`0` 留给起点/入口标记**（不是格子、没有效果）。两条轨道的第 1 格都是有效果的普通格
（内圈是机会、快车道是一个梦想格），开局若置 1 就等于全场站在机会格上却不抽卡。
`ROLL_DICE` 一次产出一整串事件：`DICE_ROLLED → （逐格，每经过一个结算格）PAYDAY/FT_PAYDAY →
PLAYER_MOVED → 落点事件`，decide 阶段在一份工作副本上逐条 `apply`，所以「先过站再落点」的账是对的；
付不出到期款项时**移动就地终止**，棋子停在那个结算格。骰子粒数**按赛道分开**（老鼠赛跑慈善 1–2 粒 3 轮、
快车道慈善 1–3 粒且永久），校验要读 `phase`。撤销掷骰整批走 `_dice_batch`（到下一条 `TURN_ENDED`
或下一次 `DICE_ROLLED` 之前的连续 seq），区间内的市场卡再并入 `_market_cascade`（两层嵌套）。
㉙ **前端另起一套骨架** `OnlineRoomView`：HUD → 棋盘 stage → 三档抽屉（peek 128px / half 46dvh /
full 88dvh），**没有 tabbar**，报表/总览/日志收进棋盘右上角的「📋 账本」。棋盘是一只 24 格（快车道 48 格）
**放射轮盘**的内联 SVG，几何常量在 `components/board/geom.ts` 一处定义；板底走 `--panel`/`--panel2`，
所以换 `.skin-ft` 金箔肤时棋盘自己跟着变金。**档位切换缩的是棋盘自身的宽度变量**，不是 `transform: scale`
（stage 有 `overflow:hidden`，缩 stage 等于白缩）。演出层 `stage.ts` 与 `receipts.ts` 同一入口
（WS `lastEvents`），所以重连首帧不带 `lastEvents`、天然不补播；点棋盘任意处**终止**序列到终态（不是加速），
`prefers-reduced-motion` 与设置里的「跳过动画」走同一条出口。
测试 **526 passed / 1 skipped**（动工前 459，新增 67 条）；`npm run ui-smoke` 扩到 **51 屏**
（线下 38 屏一屏不改 + 纯线上 13 屏）。

**真机试玩一局纯线上已完成（2026-08-08），带回 6 条待改**，设计定案已写进 design/09 **v0.2**
（逐条对照的临时清单已按其自述「改完即删」删除）：① 建房的模式二选一提为独立第一步；② 职业卡改成点牌背翻开
（服务端随机不变，补揭牌动效 + 整张 `ProfessionCard`）；③ 选梦想退回线下那套滑动实体卡，
快车道轮盘降为只读公示（格面不写字的棋盘可以公示、不能挑选）；④ 轮盘名牌从板底移到板外上方
（`bottom:5%` 压住了格子）；⑤ **一个死局**——纯线上整个没有银行入口，现金不足既买不了、
`.drawer-cta` 又被卡片按钮占满导致结束不了回合（服务端本来允许）：补账本第四页「更多」
（银行/转账/破产清算）+ 结束回合按钮常驻，并订正一条通则「**UI 的闸门不许比服务端严**」；
⑥ 起点棋子压住 12 点方向格子标签，待定。

**这 6 条（+房主追加的摇骰动效）已全部落地（2026-08-09，change `fix-online-ux-gaps`）**，
仍是纯呈现层，**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**：
㉚ **资金三块抽成共用组件** `components/tools/{BankPanel,TransferPanel,BankruptcyPanel}.vue`，
线下 `ActionTab` 与纯线上「账本 · 更多」用**同一份**——那几句规则说明（千元整数倍、月息 10%、
清偿其他负债去报表页、清算后果）只该有一个副本。线下不变靠 ui-smoke 逐屏比对兜底
（除随机房间码/日志时间戳外字节一致），并新增一屏 `03g` 专钉展开后的银行/转账。
㉛ **死局的三个出口**：账本第四页「更多」（银行仅老鼠赛跑 + 转账 + 破产入口）；
`.drawer-cta` 改成两行、**最后一行永远是「结束回合」**（判据与 `_d_end_turn` 逐项同口径——
**UI 的闸门不许比服务端严**）；破产清算面板补进 full 档（原来只有钉底一个「完成清算」，一破产就锁死）。
现金不足的三处提示（`StockTradeBox`/`OnlineCardPanel`/`OnlineLandingPanel`）改为按模式切换文案 +
「去贷款」按钮，经模块级信号 `bankrequest.ts` 打开 账本→更多→银行 并把缺口向上取整到千元预填；
快车道分支如实写「现金不够就买不了」，不指向不存在的入口。
㉜ **建房拆两步**（① 只问模式，两张竖排大卡 + 准备物清单；② 才是房间名/密码/人数，顶部回显模式可退回）；**职业卡改成点牌背翻开**（`DealCurtain` 加默认插槽，
翻牌动画与发牌共用一份，正面是整张 `ProfessionCard`，`me.professionId` 已有值时不补播）；
**选梦想退回 `SwipePicker`**，`BoardView` 的 `pickable` 连带删除，轮盘在摘要区降为只读公示。
㉝ **盘面标题移出圆盘**（`.board-wrap` 里的静态一行，`compact` 时隐藏）——**圆盘内部只画棋盘本身**；
**起点棋子挂到环外**（`geom.markerLanePoint`，位置 0 不是格子，画在环中径等于宣称大家都站在第 1 格），
有人在起点时「开始」二字让位。**骰子换成真 3D 立方体**（`Die3d.vue` + `dice.ts` 的 `DIE_FACE`/`PIPS`
两张表，对面之和为 7，尺寸只由 `--d` 驱动）：摇动是 `infinite` 多轴翻滚（不定速停，否则网络一慢
就得在最后一刻换点数）、落定转到那一面 + 回弹、多粒负 `animation-delay` 错相位；
**「还没掷」故意保留平面 `?`**——没有点数可显示时不该摆一颗看似有结果的骰子，别把两种呈现"统一"掉。
`store.lastRolls` 从视图搬进 store：开了「跳过动画」演出队列被整条丢掉，**点数是结果不是动画**，照样要显示。
㉞ **观战牌桌**：peek 条右侧头像列点开 half 档，逐人列昵称/回合步骤/现金与月现金流。
测试 **526 passed / 1 skipped**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；
`npm run ui-smoke` **57 屏**（线下 39 + 纯线上 18）。
**只差真机那一遍**：现金不足→贷款→买入→结束回合、破产清算走完、40px/58px 两个骰子尺寸看清不糊。

**第二轮试玩的 5 条已落地（2026-08-09，change `fix-online-turn-feedback`）**，仍是纯呈现层，
**引擎/协议/事件流/卡库零改动**：
㉟ **「演出成了状态的追认」是两条反馈的同一个根因**（design/09 新增 §5.0 硬规则）——WS 收到一批事件，
`ingestStage()` 刚把演出排进队列，下一行 `this.state = msg.state` 就把快照换成了「这一切都已经发生完」。
于是骰子还在转、棋子已经瞬移到目的格（走格动画一开始又跳回起点重走），牌还没翻过来、卡片已经躺在抽屉里
（帘幕落下它还在）。修法也是一条：① `ingestStage()` 排队时用 `PLAYER_MOVED.from` 把棋子**钉回移动前那一格**
（`from` 可能是 0，`positions` 用 `??` 不是 `||`，0 不会被误 fallback）；② `OnlineRoomView` 一道
`held = game.staging` 的门，关着时不出落点面板/卡面/决策按钮/`PromptModal`、不自动提档、棋盘不压暗。
发牌是队列**最后一拍**，所以门一开卡片正好在第 9 拍收牌之后落进抽屉，与节拍表逐字对上；
三条跳过出口照旧走 `skipStage()`，门立刻打开。
㊱ **自动落点必须有交代**（design/09 §4.3.1）——停在银行结算日什么提示都没有，回合突然就能结束了。
补两样东西，按**频次**分工：**落点结果卡**（`OnlineLandingPanel` 的 `landing.resolved` 分支，
讲「这一格是什么」，随回合消失、无需确认）+ **回执栈**（纯线上此前根本没渲染过 `ReceiptStack`，
别人的市场卡波及到我同样看不见）。**银行结算日/现金流量日不进回执**——它们每回合都发生，
而回执要点「我知道了」才消失，每回合逼人点一次是骚扰；这两件事的金额写在结果卡上，
回执只收低频重击（孩子/失业/税务审计·离婚·官司，后三者线下是玩家自己点的，靠既有 `SELF_ACTION`
6 秒窗口排掉，线下一条都不会多）。两样东西都会把抽屉提到 half —— peek 只有 128px，
扣掉把手/状态条/钉底按钮条后正文剩不下几十像素，留在那儿等于没给。
㊲ **账本分页控件移到内容区顶部**（`.ledger-seg`，sticky + 四段等宽 grid）：原先四个 tab 连同
「跳过动画」「收起」挤在 peek 那一条上，375 屏宽必然折成两行。状态条只留「账本」+「收起 ✕」；
「跳过动画」是**这台设备的偏好、不是账本的一页**，收进「更多」页的「🎬 显示设置」卡。
㊳ **准备页那只只读公示轮盘撤销**（design/09 §1.4.2 v0.3 订正）——快车道格面按 §3.5 不写字，
所以它是 48 个一模一样的格子里插两个小圆点，得逐格点开才知道谁是谁，信息密度低于一行字却占 332px，
还把「出牌顺序」挤出首屏。梦想归属改写进出牌顺序每人那一行「职业 · 梦想名」。
**这是本轮唯一一处线下也跟着变的地方**（那一行两模式共用，线下由「· 已选梦想」变成写出梦想名），
纯属信息增益；其余线下 39 屏一字未动。
测试 **526 passed / 1 skipped**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；
`npm run ui-smoke` **58 屏**（新增 24a「自动格的交代」，26 那屏加断言「牌还没翻过来抽屉里不许有卡面」）。
**只差真机那一遍**：掷骰棋子不瞬移、市场风云先翻牌后进抽屉、停结算日有交代、账本四页一行排开。

**第三轮试玩的 2 个 bug + 3 处手感（2026-08-09）**，仍是纯呈现层，**引擎/协议/事件流/卡库零改动**：
㊴ **轮心把「停赛中」说了两遍**——轮心那一槽本来就分两行：上面是骰盘、下面是状态提示（`hubTip`），
而停赛时上面那行被换成了另一句「停赛中 · 还需跳过 N 轮」，`hubTip` 又原样再说一遍。
删掉上面那句、只留「停赛不给骰盘」的判断：**那句话是状态提示的职责**，一个位置只有一个主人。
㊵ **账本「总览」滚到底也看不全「退出对局」**——不是滚动坏了，是抽屉的**底端落在了屏幕之外**：
`.board-page` 是 `height:100dvh` 的弹性列，HUD 约 190px + full 档 88dvh 已经超过一屏，
而抽屉写的是 `flex:none` + 定死高度，多出来的一截被 `overflow:hidden` 裁掉。
改成 `flex: 0 1 auto`（棋盘 stage 的 `flex-basis` 是 0，负空间不摊到它头上，收缩全落在抽屉上），
档位高度于是成了「最多这么高」。ui-smoke 27a 加了几何断言：抽屉底端不许出屏、滚到底最后一张卡必须露全。
㊶ **动效整体放慢一档，节奏靠对比而不是靠平均拉长**（design/09 §5.1 v0.4）：翻滚 1.3s（匀速）→
**0.62s 大幅缓出**转到那一面 → **0.65s 读数空拍**（`BEAT.diceStop`，新增一拍，骰子停稳了棋子还没起步）
→ 每格 240ms 走格 → 过站结算 1.15s / 落点 0.62s。走格的**位移时长必须比节拍略短**
（200ms < 240ms），短出来那一点就是落地的顿；追平了就成一条滑过去的直线。
`.sq-pulse`/`.sq-settle` 的 CSS 时长跟着 BEAT 走，短了会先熄灭留一段死时间。
**发牌帘幕反过来要短**：一度跟着加到 2.9s，试玩当场说「停太久」，收回 **2.05s**——
帘幕是仪式不是阅读界面，卡面随后就落进抽屉，可以慢慢看。
㊷ **职业卡「先闪一下正面」修了两处才干净**。第一处是抢跑（§5.0）：`revealing` 等
`SELECT_PROFESSION` 返回才置位，服务端状态却先到，页面已经把整张卡摆出来了，帘幕这才盖上去重翻一遍
→ 改成**帘幕先落下再发请求**，`.deal-inner` 基础 `transform` 补 `rotateY(180deg)` + `both`
（只靠 `forwards`，挂载到动画首帧之间它停在正面，照样闪），卡还没到时**不给默认插槽**
（给空插槽会让牌面高度塌成 0，连牌背都看不见）。**改完还是闪**——第二处才是根因：
`.curtain` 是 **420ms 淡入**的，帘幕变实之前，页内那张已经渲染好的正面**从半透明的帘幕底下透了出来**。
所以「翻开态」那一支加 `&& !revealing`，揭牌期间背后留着牌背。
通则：**帘幕落下期间，帘幕背后不该有它自己要揭晓的那个东西**（对任何全屏过场都成立）。
ui-smoke 20b 钉死：帘幕在场时页内 `.pcard` 数必须为 0。
顺带补上 §5.4 一直没实现的降级：`skipAnim`/`prefers-reduced-motion` 下不揭牌，直接给翻开态。
㊸ **骰子：四态同色 + 小角度斜置**。① 上一轮加的 14°/16° 斜置被当场否掉（「那个斜斜的角度不好看」），
收到 `rotateX(-10deg) rotateY(12deg) scale(.9)`、透视从 4 倍边长调柔到 6.5 倍——只露很薄的一条边，
像正面拍的一颗真骰子；`.face` 的受光内阴影保留，相邻两面的明暗交界才是立体感的来源。
② **可掷不再把整颗染绿**：绿是「能点」的信号，而掷出去那一刻 `turnDiceUsed` 就把它撤了，
于是同一颗骰子在一次动作里从绿变白，变色被当成状态变化去读。改成骰子**始终象牙白**，
「能点」由它周围一圈主色光晕呼吸（`dieCall`）来说。
③ 光晕**必须画在 `.die3d` 自己身上**（`box-shadow`）：`.die3d` 有 `perspective`，
垫一个 `::before` 在里面 Chromium 直接不画——尺寸、渐变、动画全对，就是不上屏。
测试 **526 passed / 1 skipped**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；
`npm run ui-smoke` 全过（新增 20b「帘幕底下不许有正面」，清空目录后一次完整跑产出 **61 张**——
24a「自动格的交代」这类走到才截的屏此前没被计进「屏数」，之前记的 57/58 是漏数，不是掉了屏）。
**只差真机那一遍**：新节奏是否还嫌慢/嫌拖、职业卡翻牌不闪、40px 的小骰子看不看得出立体。

**职业卡「闪一下」的第三处已修（2026-08-10，design/09 **v0.5**）**，仍是纯呈现层,
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**：
㊹ 试玩回来说「点了背面还是闪一下，但不是原来那种闪正面」——㊷ 修的两处（抢跑、透出正面）
确实都好了，剩下的是**几何突跳**，两道闸门天生拦不住。① **主因：帘幕里那张牌在翻转途中长个。**
`.deal-back` 是 `inset:0` 贴着文档流里的 `.deal-face`，牌面高度一变牌背跟着变；而职业卡这条路径
就是「帘幕先落下、请求还在路上」，`myProfession` 到了才把插槽换进去（WS `state` 广播先于 `ack`），
占位卡面只有一行标题（实测 60px）、真卡 444px，于是一条扁牌背在 `deal-flip` 的前 45%（还是**背面**
朝外）忽然长成一整张。修法不是给换入加过渡，而是**根本不在翻的过程中改几何**：`DealCurtain` 加
`ready`（`!!slots.default || !!props.card`），卡到齐了才加 `.flipping` 起翻，没到就停在 §5.4
拍 1「牌背待命」——本地几十毫秒，正好把这一拍演实；占位卡面同时撑 **3:4**（与 `.prof-back` 同比例，
400px），剩下 44px 的差换在动画首帧 `scale(.55)` 上看不出来。牌堆发牌那条路径 `card` 一挂载就有值，
`ready` 恒为真、逐帧不变。② **次因：420ms 淡入期间页内那张牌背还在抖着透出来**——
`.deal-curtain` 淡入收到 **180ms**（通用帘幕背后是无关界面，越柔和越好；发牌帘幕背后就是它自己
要揭晓的那张牌，半透明的每一毫秒都在漏），并且 `waiting` 改成 `drawing && !revealing`
（那个抖动是「请求还在路上」的提示，帘幕一落下就没有观众了）。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过，
新增 20c「揭牌起手·牌背待命」+ 一条**几何断言**（点击同帧 rAF 量 `.deal-inner` 的 `offsetHeight`，
翻完再量一次，两次之差 >120px 即判「长个」——`transform` 不进 `offsetHeight`，这条只测这一件事；
修之前是 60→444，必挂）。ui-smoke 顺带认 `UI_SMOKE_BROWSER` 与 Linux 的 `.venv/bin/python`，
Windows 那条路径仍排在最前，行为不变。
**只差真机那一遍**：点牌背到帘幕变实之间只有一张牌背、不抖、不长个。

**建房第 ① 步的选中态改成「高亮色块本身」（2026-08-10，design/09 v0.6 §1.2.1）**：
㊺ 房主反馈「线上那张顶着一块绿，看着就像已经选中了」。根因不是少了信号而是**多了一块颜色**——
两张模式卡从前是写死的一深一浅（「纯线上」= 裸 `.bigbtn` 主色实心，「线下辅助」= `.bigbtn.alt` 浅纸底），
选中态另靠描边 + 浅底光晕 + 右上角一枚 ✓ 来说。可主色实心块在这套界面里从来只有一个含义
（`.btn` 主按钮 / `.pill.on` / `.apick.on` / `.badge.turn` 全是「被选中 / 当前」），
于是屏上面积最大的那块颜色反倒不表示状态，20px 的 ✓ 说不过它；默认值又恰好是「线下辅助」，
第一眼几乎必然读反。改法是**减信号不是加信号**：两张卡默认同为浅纸底（模板上都写 `.alt`），
谁被点中谁整块换成 `--brand → --brand-dark` 渐变 + 白字，`.tick` 角标连同 CSS 一并删掉。
颜色从此只表达「这张被选中了」一件事。深底白字 ↔ 浅底墨字是**明度反相**、不只是色相差，
灰度下照样分得开，撤掉 ✓ 不会只剩颜色一条线索。裸 `.bigbtn`（深绿）不动——
大厅「创建房间 / 加入房间」两个主入口还在用它，那里绿色表示「主入口」，语义是对的。
ui-smoke 补 `18b`：点「纯线上」后 `.mode-pick .bigbtn.selected` 有且只有一张、是「纯线上」、
`.tick` 数为 0（此前 18/17 两屏从没点过任一张卡，「纯线上被选中」一次都没被截过图）。
㊻ **金箔皮肤收进对局页**（同日，上一条的截图顺带暴露：从快车道回大厅，整屏还是金的）——
换肤此前只看 `game.inFasttrack`（= `me.phase === 'FAST_TRACK'`），那表达的是**我在哪条赛道**，
只要会话与快照还活着就一直为真、**和当前在看哪一屏无关**，于是金箔从「阶段特效」变成了
跟着人走的「身份标签」，一路跟到大厅、说明书、卡牌录入。之所以没早被发现：app 内所有
「退出对局 / 房主解散 / 被移出」的出口都 `clearSession()`，快照一清 `inFasttrack` 自然变假；
**留着会话回大厅**那条路才撞得上（浏览器返回、直接开 `/#/`、从大厅点进说明书——大厅那张
「继续对局 · 回到牌桌」卡片本就是为这种状态准备的，所以玩家真会走）。修法是给条件补上
「人在牌桌上」这一半：`App.vue` 改 `watch([inFasttrack, route.path])`，只在 `/play` 挂 `.skin-ft`。
颜色变量、`.skin-ft` 的内容、`inFasttrack` 本身都没动——只改「什么时候挂这个类」。
ui-smoke 11 屏（`body.skin-ft .hud`）留作正例，16 屏补反例断言：大厅上不许有 `.skin-ft`。

**第四轮试玩的 2 处手感（2026-08-10，design/09 v0.7）**，仍是纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**：
㊼ **银行不再埋在账本第三层**——「📋 → full 档 → 分段控件『更多』→ 往下滚」三次点击加一次滚动，
而现金不够几乎总是发生在**抽屉正被卡面占着**的时候（half 档，`.drawer-cta` 上摆着「买入 / 放弃」），
要借钱得先把当前那张卡的上下文整个推开。根因是 v0.2 那次分类错了：当初把它收进账本的理由是
「与报表/总览/日志同属『随时可查、随时可用』」，可这两件事只是听起来像——**报表是读的，
资金是做的，而且总在某个具体节骨眼上要用**；排进同一叠分页，代价全由后者承担。
定案：棋盘右上角悬浮列**加第三枚圆钮 🏦**，一次点击直开「资金」弹层（`components/board/FundsSheet.vue`
= `BaseModal` 包住既有的 `BankPanel`/`TransferPanel` + 破产入口 + 显示设置，四块内容与文案一字未改）；
账本的分段控件**收回三段**（报表/总览/日志），第四页「更多」整块撤销——**一个位置只有一个主人**。
`bankrequest.ts` 的 `askBankLoan(need)` 签名不动，只把接收端从 `openLedger('more')` 改成开弹层；
三处「去贷款」提示的**用户可见文案本来就没写过路径**，所以一个字都不用改（当初没写死路径，正好省了这一遍）。
`FundsSheet` 放 `components/board/` 而不是 `tools/`：`tools/` 那三块是两模式共用的**原子**，
这一层不混进只有一种模式看得见的东西。防叠层靠 `watch(myPrompts.length)` 单向兜住——
反方向不用管，`.modal-mask` 是全屏 fixed 且压在 `.board-tools` 之上，`PromptModal` 在场时 🏦 本来就点不到。
㊽ **「收起 ✕」删掉，收起归把手**——那枚 `.btn.ghost.small` 是补位补出来的：把手从前只认拖拽
（位移不到 24px 直接 return），点了没反应，只好在状态条右端另加一个按钮；而手动把抽屉拉到 half
看牌桌的人**连那枚按钮都没有**，只能再点一次头像列。一条规则收干净：**向下 = 退一步，向上 = 进一步**，
位移不到 24px 算点击（peek 档点是展开，其余档位点是退一步）；账本打开时的「退一步」**是整个关掉账本**，
不是退到 half——账本只有 full 一种形态，半开着既读不成也让不开路。把手改成 `<button>`（视觉零变化，
裸 `button` 早已被重置成中性）并按档位给 `aria-label`；`setPointerCapture` 加 try/catch
（指针不活跃时会抛 `NotFoundError`，抓不到不该把这次交互整个废掉）。
㊾ **顺带量出一条几何事实并处理掉**：full 档下棋盘 stage **只剩 16px**——页面是 `height:100dvh`
的弹性列，HUD 加 88dvh 已超一屏，负空间**全部由抽屉吸收**（stage 的 `flex-basis` 是 0，摊不到它头上）。
三枚圆钮的第一枚就占 46px，留着只会被 `overflow:hidden` 切成一条边，**半截的圆看着像渲染坏了，
不像一个决定**，所以 full 档整列不渲染。代价可接受：full 档只在「账本打开」与「破产清算」两种情形出现，
前者点一下把手就退出来（于是「从账本去银行」是两次点击，从其余任何状态都是一次），后者本就只该有清算面板。
测试 **527 passed**（后端未动，作回归；此前记的「526 passed / 1 skipped」里那 1 条是平台相关，
Linux 上会真跑）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过，合并 main 后一跑产出 **64 张**
（满编 65，24a「自动格的交代」这类走到才截的屏这一跑没走到）。本轮新增 23b「拉到 half 后点把手
收回 peek」与 27d「资金弹层」，27c 由「账本 · 更多」改为「点把手关掉账本」；另在 22 与 27 两头
钉死悬浮圆钮的可见性：peek 档三枚必须整枚可见，full 档一枚都不许被切。
「线下一屏不变」是**跟本分支的父提交 bb63265 逐屏比对**验的（借 `UI_SMOKE_BROWSER` 那条
Linux 退路在容器里跑基线，那条退路是 main 上 ㊹ 那一轮加的）：线下 39 屏里 26 屏字节一致，
另外 17 屏的差异全部来自随机房间码/随机职业与梦想/日志时间戳，弹层本身逐像素相同。
（合并进来的 ㊺㊻ 会另外改动 16/17/18 三屏，那是 main 那一轮的事，不在这条比对里。）
**只差真机那一遍**：现金不足 →「去贷款」→ 弹层直接开在银行且金额已预填 → 贷款 → 买入 → 结束回合；
34×4px 的把手用拇指点得中吗（`::before` 热区是 64×32，理论够）。

**持续状态对同桌可见（2026-08-10，change `show-persistent-status`）**，纯呈现层，
**server 一个字没改**（协议/事件流/卡库/引擎全部零改动）。根因是**数据早就下发了，缺的是呈现**：
`serialize()` 的 `viewer_id` 形参从未被使用，每个人的 `charityTurns`/`skipTurns`/`inBankruptcy`/
`childCount`/`fasttrack.charity_forever`/`installmentReceivables` 本来就广播给全员。可**慈善对别人
一处都看不到**，停赛只在纯线上牌桌（且只在非我回合渲染）与总览页各一处，同一句话还在四五处各写一遍
——停赛在线下行动页是红 `.badge.out`、在总览页是灰 `.badge`，措辞与配色已经打架。
㊿（**圈号到此为止**，Unicode 这套编号最大就是 ㊿；再往后请另起编号方式）
① **一处派生 `statuses.ts`**：`playerStatuses(p)` 按优先级给出八种状态
（出局 > 破产清算 > 停赛 > 慈善 > 快车道慈善 > 快车道 > 分期收款 > 孩子数），
`major` 决定进不进牌桌与座次条；`majorStatus(p)` 给只画得下一个记号的地方。
**两条赛道的慈善是两条**（老鼠赛跑 3 轮 1–2 粒 P.4 / 快车道永久 1–3 粒 P.6），不合并；
**停赛不写来源**——引擎只存 `skip_turns`，2 轮来自失业、3 轮来自破产复活，状态里没这个信息，
猜出来的来源迟早会错。颜色统一：红只留给出局/破产清算，停赛改中性灰。
② **两处照抄的代码收成组件**：`PlayerTableRow`（线下 `ActionTab` 与纯线上牌桌本就逐行相同，
design/09 §6 原话是「用既有那套，不新写」，当时其实抄了一份）、`SeatStrip`（HUD 与 peek 条
各写了一遍）。牌桌的步骤文案遇到出局/破产/停赛改**返回空串**，那三句归徽章——
与 v0.4 修掉的「轮心把停赛说了两遍」同一条：一个位置只有一个主人。
③ **座次条角标**（`.seat-dot .mark`，7px）：座次条自称能读出「这一轮还剩几个人到我」，
可有人停赛时这句话就是错的——被跳过的人根本不会轮到。而 `skip_turns` 是在 `_advance_turn()` 里
**静默递减**的，那段代码跑在 apply/重放阶段、**发不出事件**，所以「谁被跳过了」没有事件可听，
只能由状态本身说出来。
④ **状态开始时给全员一条回执**（`receipts.ts` 加 5 个 case）：范式照抄 `ENTERED_FASTTRACK`
——有确定的 `player_id` 就用它排除当事人，不进 `SELF_ACTION` 那张 6 秒时间窗的表。
失业只加「别人」那一支（当事人那支与 `SELF_ACTION` 条目都不动，所以**线下一条都不会多**）；
慈善两套模式都是自己点的，靠 id 排除即可。`BANKRUPTCY_RESOLVED` 的结局（复活/出局）写在
服务端 apply 里、payload 看不出来，所以 `buildReceipts` 加了个**可选** `next`（新快照），
由 `store.ts` 的 `ingestEvents` 传入——读的仍是服务端算好的结果，客户端不重算规则。
别人生孩子/分期推进/结算日一律不进回执（design/09 §4.3.1 的频次判据）。
`npx vue-tsc --noEmit` 通过；后端 `pytest` **527 passed** 作回归；
`npm run ui-smoke` 全过（合并 main 后一跑产出 **69 张**；新增 03h/03i/03j 线下三屏、
23c「没人有状态时一个角标都没有」、
29「三人房里把一人标成出局」的正例——纯线上没有别的确定路径能造出持续状态，
靠掷骰走到慈善格是碰运气，跑四次只中一次，不算验证）。
**只差真机那一遍**：19px 座次点上的 7px 角标看不看得清、回执会不会觉得吵。

**结算日改成全屏发薪帘幕（2026-08-11，change `payday-curtain`，design/09 **v0.9**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**。房主反馈「经过现金流量日只有一个
小小的冒泡提示，不够明显」——那个提示是轮盘上一个 **13px** 的飘字（`.settle-amt`），1.15s 播完零残留。
⑤ **根因是仪式感的排序和游戏机制的排序反了**：同一套界面里抽一张小生意卡有 **2.05s 的全屏帘幕
加翻牌**，而所有投资努力兑现的那一刻只有一个飘字。判据补进 design/09 §5.5：**一件事该给多大的
呈现，看它在游戏机制里有多重，不看它实现起来在哪一层**（飘字之所以是飘字，只因为它当初是走格
动画的附属品）。改法是新增 `components/board/PaydayCurtain.vue`——复用 `.curtain` 基座与
`FasttrackIntro` 那套 `.cline`，老鼠赛跑写「工资 / 非工资 / 总支出 → 本月净得 → 银行储蓄 旧→新」
（引擎的 `total_income = salary + passive_income` 保证这三行**加得平**，屏上可验算），
快车道是金箔底 + 六位数大字，月现金流为负时**底色转冷 + 📉 + 「本月净支出」**。
`BEAT.settle` 1150 → **1700**（`.sq-settle` 的 CSS 时长跟着走）。
⑥ **帘幕不给「知道了」按钮**——`FasttrackCheer`/胜利屏一局只看几次可以要求确认，这一屏一局要
看十几次，每次逼人点一下是骚扰；点任意处跳过走 `skipStage()` 同一条出口，`skipAnim`/`reduce`
下 `ingestStage` 那条整条丢弃的出口天然覆盖，**降级一行都不用写**。
**只给当事人**（别人的回合也弹全屏就是刷屏），且帘幕在场时板上不再飘同一个数（同 ㊷ 那条通则）。
**破产那一批不弹**：付得起的月份照样入账、拍还在，但清算屏之前不该先演一场庆祝仪式。
⑦ **屏上的数在排队时就从「结算前」的快照里焊死**（`buildStage` 的 `prev`）——权威状态在演出
开始前就已经是结算后的样子，播放时再读，「$2,100 →」的左边那一半就没了。收支结构不受结算日
影响，所以三行明细取新旧快照是同一组数，**唯独现金必须是旧的**。
⑧ **帘幕散场后要留存根**（§4.3.1 改写）——帘幕自动消散、还能被跳过，**播完零残留**；
而「经过」根本不产生 landing，落点结果卡只认「停在」，于是「经过 + 没看清」此前没有任何回看的
地方。所以 `PAYDAY`/`FT_PAYDAY` 从结果卡的 switch 里**划走**，改由 store 的 `lastSettlement`
（`catchStub`，与回执同一入口）派生一张存根卡，金额取**事件**不取快照——一次移动可能连过两个
结算格（`test_online_board.py` 早就钉死会产出两条 `PAYDAY`），快照里只有单月值，「×2」看不出来。
回执仍然不收结算日，那条频次判据不变。
⑨ **旁观者两件事**：板上飘字 13px → **20px**，并修掉一个一直在的 bug——`+` 号是硬编码的
（`BoardView.vue`），月现金流为负时显示成 `+-500`，颜色还写死 `--pos` 绿色；
另给座次条加**瞬时金额角标**（`.seat-pay`，纯从 `stageNow` 派生、不落 store、**不进
`statuses.ts`**——那份是「持续状态」的主人，这个随那一拍生灭）。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过，
新增 24b（发薪帘幕）/ 24c（别人过结算日）/ 24d（结算日存根）三屏。这三屏和 24a 一样是
**「走到才截」**——哪一回合过站由服务端点数决定，而 24 格里一半是机会格，第一次掷骰就撞上
循环当场就 break，那一跑就一次都不过站（所以一次完整跑产出 69 或 73 张）。
**试过让循环「没截到发薪帘幕就把机会卡就地抽掉、继续掷」：结算日三屏稳了，25/26 那四屏
反过来饿死，还把牌局留在半路上害 27e 挂掉——一个循环不该同时扛两个目标**，已回退。
**ui-smoke 顺带补了一个 `shotNow()`**：`shot()` 是「等元素 → 停 500ms → 截 → 再验」，
而这一拍只在场 1.7s，截出来的图是对的、断言却报「没渲染出来」——**只在场一两秒的屏，
断言必须在发现的那一刻就把 DOM 取到手**（截图则要再等 0.85s，等明细逐行入场排完，
否则拍到的是只显示前两行的半成品）。
「线下一屏不变」是跟父提交 9e52c87 **逐屏比对**验的（`UI_SMOKE_BROWSER` 那条 Linux 退路 +
git worktree 跑基线）：69 屏里 33 屏字节一致，其余差异全部来自随机房间码 / 随机职业与梦想 /
日志时间戳（逐张量过差异区域，线下 `03*` 行动页那一组一张不差）。
**只差真机那一遍**：1.7s 会不会嫌长、负现金流的冷色版、六位数大字在 375 屏宽不折行、
别人过结算日时座次条那枚角标吵不吵。

**第五轮试玩的三处（2026-08-11，design/09 **v0.10**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**：
⑩ **说明书先闪一句假话**——点「📖 说明书」会先看到「尚未生成说明书分页图，请去跑
`build_manual_pages.py`」那张卡，然后才出现说明书。`ManualView` 的 `pages` 初值是 `[]`，
模板是 `v-if="pages.length"` / `v-else` 的二选一，**没有 loading 这一态**，于是 fetch 返回之前
那句话必然先渲染一帧——而图明明生成好了，等于每次进说明书都先给一句错话。补 `loading`，
**载入期间两支都不渲染**（分页图随镜像交付又被 PWA 预缓存，这一帧通常零延迟；
摆一句「正在载入…」只是把一闪而过的噪音换成另一种）。顺带给 fetch 补 try/catch：
「拉不到」和「真的没生成」分开给文案，断网时不再叫人去跑构建脚本。
⑪ **抽屉的手势铺到整块面板**（design/09 §2.2 重写）——从前只有那根 34×4px 的把手接手势，
在正文里往下拉换来的是**浏览器把整页刷新掉**。v0.7 那次把把手从「只认拖拽」补成「也认点击」，
解决的是「点了没反应」；把手势限死在把手上是当时省下的一步。现在三处入口共用一台跟手状态机
（拖到哪儿高到哪儿，松手就近吸附）：把手跟手 + **认点击**（peek 档展开 / 其余退一步，
账本的「退一步」是整个关掉），状态条与正文只跟手不认点击。**「一个位置只有一个主人」说的是
点击那件事，不是说跟手也只能有一个入口。** 位移不到 24px 一律不换档——拿「有没有 move 事件」
当判据的话，手指点一下带三五像素就成了拖一下，什么都不会发生。
**正文的主人是滚动**，抽屉只在滚不动的方向上接管（向下且 `scrollTop<=0` / 向上且正文压根滚不动），
其余归滚动**且这一次触摸不再重判**（滚到顶突然被接管，比拖不动更糟）。正文用 touch 事件而不是
pointer：要赶在浏览器判成滚动**之前** `preventDefault()`，而 `touch-action: pan-y` 下滚动一开始
pointer 就被 `pointercancel` 掉了；`touchend` **也要** `preventDefault()`，否则松手时浏览器
朝手指底下补一次 click——正文里全是「买入 / 放弃」。
⑫ 顺带立一条通则：**页面自己不响应的手势，也要挡住不让它溢出给浏览器**。滚不动的方向上
什么都不做，不等于什么都没发生——滚动链一路溢出到根就是下拉刷新。`html, body` 给
`overscroll-behavior-y: none`，`.drawer-body` 给 `contain` + `touch-action: pan-y`，
不滚动的 `.drawer-peek`/`.drawer-cta` 给 `touch-action: none`（拦的是浏览器平移手势，点按不受影响）。
⑬ **「跳过动画」那个复选框被撑成铺满整行的巨大方框**——`style.css` 的通用表单样式
（`width:100%` + 44px 热区 + 16px 防 iOS 聚焦缩放 + 内边距描边）说的是**文本输入框**，
却把 `input` 一整个选择器都罩了进去；全仓库只有 FundsSheet 这一个 `<input type="checkbox">`，
之前从没撞上过。选择器收窄成 `input:not([type=checkbox]):not([type=radio])`（以后再加也不会重蹈），
另给一枚 `.switch` 开关样式。**仍然是原生 checkbox**：外层已经是 `<label class="row between">`，
点整行切换、键盘可达、`:checked` 驱动全是现成的，换成 `<button>` 反倒要各补一遍。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过，
新增 23d「拖正文收抽屉」（合成 `Touch`/`TouchEvent` 直接派发，不依赖 context 的 `hasTouch`；
除断言抽屉变矮外还钉死「这一拖不许顺手点掉正文里的东西」）+ 27d 补一条几何断言
（复选框 `offsetWidth` 必须 < 60，修之前是整行宽）。
「线下一屏不变」是跟父提交 73c6aeb **逐屏比对**验的：线下 47 屏里 33 屏字节一致，
另 14 屏的差异全部来自随机房间码 / 随机职业与梦想 / 日志时间戳（线下 `03*` 那一组
连同 `03g` 银行转账面板一张不差）。
**只差真机那一遍**：进说明书不再先闪那张卡；抽屉正文往下拖能收起且页面不刷新、
往上拖能展开、正文长到能滚时上拖仍是滚内容；拖完松手不会误点到底下的「买入 / 放弃」；
「跳过动画」是一枚开关且点整行能切。

**职业卡的翻面动画修回来了（2026-08-11，design/09 **v0.11**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**。房主反馈「开局抽卡怎么没有翻面的动画？
点一下背面，背面变小，然后又稍稍变长，然后突然出现那个正面」。三段各有各的来历，主因在第一条：
⑭ **翻牌动画其实一次都没播过。** v0.5（㊹）那道「卡面没到就不起翻」的闸门写成了
`computed(() => !!slots.default || !!props.card)`——而 `useSlots()` 拿到的是组件实例上那个
**普通对象**，父组件重渲染时它被**就地改写**，没有任何响应式依赖可追踪。职业卡这条路径上
`props.card` 恒为 undefined，于是 computed 第一次算出 `false` 就**永远**是 `false`，
`.flipping` 一辈子加不上：屏上只剩基础 transform 那张 `scale(.55)` 的牌背（=「背面变小」），
卡面换进来时 `.deal-face` 高度 400→444 带着 `inset:0` 的牌背一起长（=「又稍稍变长」），
帘幕一撤页内正面直接顶上来（=「突然出现正面」）。改成渲染时求值的函数。
通则：**`useSlots()` 的结果只能在渲染时求值，不能进 `computed`。**
⑮ **两条节拍表不该共用一段 keyframes。** `deal-flip` 编的是 §5.1 的拍 6「飞入放大 0.43s」+
拍 7「翻牌 0.52s」，而 §5.4 职业卡**根本没有飞入拍**——牌已经在屏上被玩家点了。
`DealCurtain` 加 `variant`（`deal` / `reveal`），职业卡走新的 `card-reveal`：待命态与页内那张
`.prof-back` **同尺寸**（都是 `76% / max 300px`，只有「飞到屏心」没有「变小再变大」），
整 0.95s 全给旋转。复用共用的是 **3D 结构、牌背材质、帘幕基座**，`@keyframes` 不在其中。
⑯ **翻转读不读得出来，看的是 90° 交界点落在哪、透视有多强，不是看时长。**
`backface-visibility` 的正反切换就发生在 90° 那一帧，而 `--ease`（重度 ease-out）把它压到
翻牌段的头 20%（≈104ms），剩下的时间只是正面慢慢摊平——**时长再长也是一次硬切**。
配速改为**逐 keyframe 给**（`animation-timing-function` 写进关键帧），交界点摆到整段正中；
`perspective` 900px → **700px**（卡宽只有 300px，3 倍焦距几乎没有近大远小）；
`translateZ` 的抬起**必须写在 `rotateY` 前面**（写后面位移就跟着卡自己的法线走，转到 90° 时
成了往侧面推），在 90° 处最高，卡「立起来」那一瞬才不是一条零宽的缝。
⑰ **几何「全程不变」做实**：不再靠占位卡面去凑真卡高度（占位 3:4 = 400px、真卡 444px，
缩放一撤那 44px 全暴露），改由 `.deal-inner.reveal` **自己**焊死 3:4、正反两面绝对定位铺上去——
牌的外框不再由卡面驱动，换卡那一帧一个像素都不动。ui-smoke 20b 的容差随之从 **120px 收到 4px**。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过
（69 屏）。新增 **20d「翻转真的在转」**——这才是这次的正主，以前一条断言都没钉住它：
把动画**暂停后逐点 seek**（`anim.currentTime = t` 再读 computed style，结果只由 CSS 决定，
与真机快慢、截图停顿无关），沿 33 个采样点读 `matrix3d` 的第 11 个数（= `cos θ`，**不含缩放**），
断言「侧过来的帧必须是 matrix3d」「cos θ 从 −1 扫到 +1」「三成时间过去必须已起转」
「半程 cos θ ≈ 0（交界点在正中）」。**做过负向对照**：把 CSS 退回旧配速后 20d 当场报
「三成时间过去了牌还没起转（cos θ=-1.000）」。20c 那屏改名为「揭牌-翻转途中」——
`ready` 修好之后卡面本地几十毫秒就到，拍 1「牌背待命」只在服务端慢时才看得见。
**只差真机那一遍**：0.95s 的翻转是否嫌慢、能不能看出近大远小、牌背与正面的一大一小还在不在。

**牌背在翻转前那一次「变大小」已消掉（2026-08-12，design/09 **v0.12**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**。房主反馈「不管是抽职业卡还是抽 4 种
功能卡，卡的背面都会突然变一下大小，然后又翻转」。**同一个症状，两条路径各有一个来源，
根子是同一条**：§5.4 那条「过场里的主体，几何要么全程不变、要么只在它自己的动画里变」
只落实到了帘幕**内部**，没落实到「玩家上一眼看的那个东西 → 帘幕里那张牌」的**交接**。
⑱ **「飞入」从来没有实现过。** §5.1 拍 6 写的是「牌背**从格子位置**飞向屏心并放大」，
而 `deal-flip` 里只有 `scale(.55)→scale(1)` 的原地放大——`stage.ts` 每次发牌都算好了
`fromIndex`，`OnlineRoomView` 只传了 `deck/title/card`，这个字段一路没人用。
**没有起点的放大不是运动，是一次尺寸突变**。改法：`geom.ts` 加 `squareViewportRect()`
（由 `<svg>` 外接矩形线性换算，viewBox 是正方形不必动 `getScreenCTM`，几何仍只在一处定义），
`BoardView` `defineExpose({ disc })`，`OnlineRoomView` 按 `fromIndex` 算出源矩形传给帘幕。
顺带修了 `landingIndexOf` 的两个错：① 掷骰那一批里 `prev` 的位置还是**掷骰之前**的，
会从上一格飞出来；② 「停在机会格 → 点小生意」那一批里只有 `CARD_DRAWN`、一个移动事件都没有，
批内的 `mover` 从没被置过，`fromIndex` 恒为 0（**26d 一开始就是被这条钉挂的**）。
现在落点优先取本批 `PLAYER_MOVED.path` 末尾，没有移动才回 `prev`，赛道取玩家自己的 `phase`。
⑲ **职业卡那半是跨元素的交接没对齐**：页内 `.prof-back` 与帘幕里 `.deal-card` 都写着
`76% / max 300px`，但**百分号的分母不是同一个**——前者的父容器是 `.page`（`#app` 的 560px
再减 12px 内边距），后者是 `.curtain`（`fixed; inset:0`，**不经过** `.curtain-inner` 的限宽）。
390px 手机上是 278px 对 296px，位置更是一个在页面流里、一个钉在屏心；`.deal-curtain` 只有
180ms 淡入，而页内那张在 `revealing` 期间走 `v-else` **全程还在屏上**，两张同款牌背交叉淡入。
≥560px 的屏上两者都被 `max-width:300px` 截平，**所以这个 bug 只在手机上看得见**。
修法不是把两条 CSS 对齐（那只是让这一次的数字碰巧相等），而是点击那一刻量下源矩形当锚点；
页内那张同时 `visibility:hidden` 交班（**不是 `v-if`**，留着占位才不会在帘幕底下重排）。
⑳ **实现上把飞入与旋转拆到两个元素**：`.deal-card`（透视层）飞、`.deal-inner` 只转。
位移**不能**发生在透视层的孩子身上——`perspective-origin` 钉在 `.deal-card` 中心，
把孩子平移出去就带上斜切，而从棋盘边缘到屏心是一两百像素。`deal-flip` 拆成 `deal-fly`
（两个 variant 共用，只差时长：牌堆 0.43s / 职业卡 0.28s）+ `deal-spin`/`card-reveal`
（纯旋转，`animation-delay` 接在飞入之后）。`.deal-card` 的**基础 transform 就是起飞姿态**
（同「静止态就是牌背」那条路子），拿不到锚点时兜底值退回原地放大，不产生新的失败模式。
㉑ **一个踩过的坑**：量自己的终态宽度**只能用 `offsetWidth`**——此刻基础 transform 里的
`scale(var(--fs,.55))` 已经生效，`getBoundingClientRect()` 给的是缩过的框，拿它当分母算出的
`--fs` 会大出 1/.55 倍（首帧 506px vs 应有的 286px）。中心点不受影响（`transform-origin`
在正中，缩放不挪中心），照旧取 rect。
㉒ **几何锁没有跟着提到 `.deal-inner` 上**（计划里写了，动手时否掉）：职业卡正面只有一种、
高度是定的，焊 3:4 没问题；而 194 张牌堆卡的正面由正文行数决定（300~500px），焊死之后
**正面翻过来会比刚才那张牌背高一大截**——那是把「翻转前变一下大小」换成「翻转后变一下大小」，
同一个毛病换个位置犯。牌堆卡的牌背仍跟着真卡走；卡面晚到那一跳已由飞入拍化解
（待命那一帧牌缩在棋盘那一格上，`--fs`≈.3，此时改高度几乎看不出来）。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过（74 屏）。
新增 **20e / 26d**——这次的正主，现有断言一条都钉不住它（20b 量的是 `offsetHeight`、
`transform` 不进布局；20d 特意把缩放从 `cos θ` 里约掉了，两条对位移与缩放都免疫）：
把飞入动画 `pause()` 后 seek 到首末两帧量 rect，20e 断言「首帧必须压在被点的那张牌背上」
（中心差 < 6px、宽度差 < 6px），26d 断言「首末两帧的中心必须差开」+「在格子环上」+「起飞够小」。
**20d 也必须跟着改**：旋转拍现在带 `animation-delay`，而 `anim.currentTime` 是从**延迟开始**
算的，照旧 seek 到 `[0, dur]` 会全部落在延迟里（每帧都是 180°），「三成时间过去必须已起转」
当场误报——改成先读 `getComputedTiming().delay` 再按 `delay + dur*i/32` 采样。
**做过负向对照**：把 `:from` 改成常量 `null`，20e/26d 当场报「首帧没压在被点的那张牌背上
（中心差 36.4px）」与「牌背没有位移（首末两帧中心只差 0.0px）」。
26d 的第一版只查「首帧落在圆盘的外接矩形里」，**负向对照下照样通过**（屏心离盘心正好
0.82 个半径，落在环带里）——**位移才是这一拍的主语**，所以判据换成首末中心必须差开。
「线下一屏不变」是跟父提交 ccf50c0 **逐屏比对**验的（`UI_SMOKE_BROWSER` + git worktree
跑基线）：74 屏里 30 屏字节一致，**线下 `03*`–`15` 那一整组一张不差**；其余差异逐张量过
差异区域，全部来自随机房间码（`01/02/14/16~19` 只差页头那一条，房间码字数不同导致整页
下移一行的也在内）、日志时间戳（`08*` 是固定间隔的窄横条）与一条没散尽的 toast（14）。
**只差真机那一遍**：点职业卡牌背 → 那张牌从原地起飞、不先跳一下大小；掷骰落到抽卡格 →
牌背从**那一格**飞出来；375px 窄屏与平板各看一次（尺寸差只在窄屏上存在过）。

**第六轮试玩的四条（2026-08-12，design/09 **v0.13**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**——但**「线下辅助模式一屏一字未变」这句话本轮第一次不成立**：
`OverviewTab` 与 `PlayerTableRow` 是两模式共用的组件，所以线下总览页跟着去掉红框、
线下行动页那一行牌桌跟着变丰富（房主已定案照收：两处都是信息增益或减噪、不改任何点击路径；
要坚持线下不动就得各拆一份副本，而那正是 v0.8 刚合并掉的重复）。
㉓ **「跳过动画」开关整个撤销**（房主：「放在银行的悬浮菜单里面不合适，大家还是喜欢看动画的」）。
根因不是它放错了层，是它**本就不该是持久开关**——玩家想跳过的是**这一次**已经看过二十遍的
演出，不是**此后每一次**，而「点棋盘任意处终止到终态」早就办到了，还不留状态。
**删按钮不能留下一把锁**：只删 UI 的话，开过它的设备会永远停在「不播动画」而界面上再没有
地方关掉，所以 `localStorage['cashflow.skipAnim']` 的**读取也一起停**
（`loadSkipAnim`/`saveSkipAnim`/`store.skipAnim`/`setSkipAnim` 全删，三个读点只留
`prefersReducedMotion()`）。通则：**一个偏好的读与写要同生共死**。连带 `.switch` 成了死 CSS
一并删除，但 v0.10 那条 `input:not([type=checkbox])` 的收窄**留着**——它修的是类型错误，
与眼下有没有勾选框无关。
㉔ **总览页三处红全部撤销**（房主：「移除玩家和退出对局的那个红色菜单好丑啊」）。
**红被当成了「这里有个功能」的标记，而它只该表示「这一步不可逆」**——总览是一页读物，
屏上最扎眼的三块颜色却给了一局最多点一次的操作；而二次确认里那句「此操作不可撤销」本来就
是红的，同一件事说了三遍。改法是**减信号不是换颜色**：两块红框卡合成页尾一行安静的文字链
（`.quiet-links`），「移除玩家」移进**点开的记录卡弹层**的 `#actions`（针对这个人的处置，
上下文就是他的记录卡；移除成功后弹层自己退场）。顺带修一个真 bug——
`class="btn small ghost warn"` 里 `.btn.ghost` 排在 `.btn.warn` 之前、特异性相同，
**后者胜出**，写着 ghost 一直渲染成小号实心红；新增 `.btn.ghost.warn` 把这个组合定死。
㉕ **牌桌升为显式内容态**（房主：「基本上就没有机会看到，一旦他抽了卡之后，就会被那个
卡片的事件覆盖」）。**根因是牌桌没有状态位**——破产面板有 `inBankruptcy`、账本有 `ledger`，
只有它是「默认层里恰好没别的东西」时的兜底（`!isMyTurn && (held || !activeCardInfo)`），
而 `activeCard` 活到**回合结束**，能露脸的窗口只剩「掷骰 → 落点」那几秒；更要命的是座次条
的 `@open` 改的是**档位**不是内容，「点头像列看牌桌」这个心智模型从来没对齐过。改为与账本
同一范式：点座次条（HUD 与 peek 条两处都能点）→ `table = true`，牌桌**压过卡面与落点卡**、
提到 half，座次条加一圈主色光晕；再点 / 下拖 / 轮到我 / 有 prompt 则关。
**主动打开的东西盖住被动内容**是既有规矩（同 `FundsSheet`）。回执在牌桌态下**照旧渲染**——
它是「刚刚发生在你身上」的事，不能因为我在围观就被吞掉。否决「排在卡面下面」（half 档下整块
在折叠线以下）与「升为账本第四页」（v0.7 刚以「一个位置只有一个主人」撤销过第四页，且账本
是读物、牌桌要瞟一眼、总览已在账本里会当场重复）。
㉖ **牌桌一行加进度条与家底计数**，判据是**牌桌多的是「此刻」，总览多的是「可点进去」**
（否则两者趋同）。进度口径与总览页同一份、股票按**总股数**汇总（`stocks` 是批次数组，
数条目数出来的是「买过几次」）；出局/破产清算的人不画进度与家底，处境归状态徽章
（停赛照画——那只是这几轮不能动，账面照常在跑）。**「棋子在第几格」被房主否掉**：
棋盘上那颗棋子已经把它画出来了。
㉗ **HUD 末行新增可展开资产条**（房主：「加上自己的资产展示，房子啊股票之类的，简单一点」）。
收起是一行 `🏠×2 🏢×1 📈1,500 股`，**一项都没有就整行不渲染**（开局摆「暂无资产」是废话，
买下第一项时多出这一行本身就是信号）；展开**不撑高 HUD**，是贴 stage 顶落下、压在棋盘上的
浮层（`.asset-pop`，40dvh 内滚，点浮层外收起）——stage 的 `flex-basis` 是 0、full 档下实测
只剩 16px，HUD 一长高先被挤没的就是棋盘，**过场与浮层可以叠上去，几何不动**。
快车道下改列 `fasttrack.businesses`（记录卡已翻面）。
测试 **527 passed**（后端未动，作回归）；`vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过
（合并 main 的 v0.12 之后一跑产出 **73 张**，满编 80——24b~24d 与 26f/26g 那几屏是
「走到才截」，这一跑既没过结算格、抽到的牌也买不起）。改 27d 断言（`hasNot: 跳过动画/显示设置` + 全页勾选框数必须为 0），
新增 22b「开局无资产不画资产条」反例、03k「总览页安静的出口」（红框卡与红实心按钮各为 0）、
03l「非房主的记录卡弹层没有移除玩家」、26e「牌桌压过卡面」（有 `.ptrow`、无 `.gcard`）、
23a 补「每一行都有进度条 + 座次条有选中态」、26f/26g「HUD 资产条与明细浮层」
（含几何断言：展开前后 `.board-stage` 的高度不许变）。
**26f/26g 那两屏是「走到才截」**——纯线上抽到哪张牌由服务端定，没有别的路能让玩家名下确定地
长出一项资产，所以在 26a 那一拍「买得起就买」，买不成就照旧点「不要 / 付掉」。
（`26d` 已被合并进来的上一轮占用——那是牌堆卡飞入那组断言，本轮这三屏顺延到 `26e`–`26g`。）
「线下的改动只有那两处」是跟本分支的父提交 ccf50c0 **逐屏比对**验的：线下 41 屏里 26 屏字节一致，
15 屏有差异——`03f`/`03i`/`12b` 是牌桌那一行多出的进度条，`07`/`03j` 是总览页去红，
其余 10 屏（`01*`/`02*`/`08*`/`16`）全部来自随机职业与梦想 / 日志时间戳 / 随机房间码。
**只差真机那一遍**：点座次条能不能顺手调出牌桌、再点收回；HUD 那行资产点开的浮层压在棋盘上
会不会挡视线；总览页那行文字链是不是太安静了找不着。

**自己回合停赛时「跳过本回合」按钮已撤销（2026-08-12，design/09 v0.14）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**。房主反馈「自己回合进入失业时，跳过本回合不恰当」。
根因是这个特殊分支的前提本身不成立：`_advance_turn`（`server/app/engine/engine.py`）对
`skip_turns>0` 的玩家是静默递减 + continue，从不 `break` 让他们成为 current player，
所以 `skipTurns>0 && isMyTurn` **只可能在停赛刚生效的那一回合成立**（失业已结算 `1951` 行 /
破产刚复活 `1989`/`2003` 行）——玩家这一回合正常掷了骰、走了格、付了钱，根本不是
"什么都不能做只能跳过"；真正会被跳过的后续几轮，玩家从来不会被推成当前玩家，前端连按钮
都不需要。原实现（`OnlineRoomView.vue`）却把这一回合的结束回合按钮换成语义错误的 ghost
「跳过本回合」，还绕开了 `endTurn()` 的确认弹窗；抽屉正文里同时多插一张「停赛中 · 还需
跳过 N 轮，这一回合只能跳过」卡片，与 `OnlineLandingPanel` 已经在渲染的「停在失业格 ·
已支付一次总支出，并停赛 2 轮」结算卡重复，且未结算时"只能跳过"这句话是假的（旁边就摆着
「支付失业损失」按钮）。改法是把这两处特殊分支整段删掉，让这一回合和任何回合走同一条路径：
`blockedBy` 早就会在未结算时禁用结束回合、结算后自动放行显示「✅ 结束回合」——与线下
`ActionTab.vue`（从来没有这个特殊按钮）保持一致。停赛状态的可见性不受影响，继续由
`hubTip`、`statuses.ts` 的 `majorStatus`（座次条/牌桌/总览角标）独立展示。
测试 **527 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke`
全过（79 屏，本次改动不涉及任何截图断言）。

**棋盘重构：两条赛道一张板（2026-08-12，design/09 **v0.15**）**，纯呈现层 + **一处引擎状态**
（`ft_sold_squares` 升成 dict、新增 `ft_claimed_dreams`，纯追加可重放）。房主试玩快车道带回两条：
「看不到慢车道其他玩家的绕圈状态」「快车道没有文字，颜色也对不上，买过的企业没有占位道具」。
⑮ **跨赛道看不到，根因不是「没画」，是「画了一个假位置」**——`positions` 按**观看者自己**的赛道
挑字段，不是按被画玩家的 `phase`：快车道玩家看到的老鼠赛跑同桌 `ftPosition` 恒为 0，被画到
「在此进入」标记旁；反过来老鼠赛跑玩家读到的是对方逃出那一刻**冻结的** `rrPosition`，看着像一个
永远不动的人。数据本来就对全员广播（两个独立字段），缺的只是按被画玩家取 + 两条轨道同屏。
⑯ **棋盘改成内轮盘 24 格 + 外圈圆角方形跑道 48 格**（实体棋盘本来就是这个结构，正圆盘四个角一直
空着）。快车道一格 **17.2px → 23.5px**，7 个特殊格写得下 2 个字，企业/梦想印图标。**半径链从
空档带往回推**：两条轨道各有一批「还没上路」的人（老鼠赛跑位置 0 是开局全员、快车道位置 0 是刚
逃出来的人要停留整整一个回合），棋子直径 18 单位，所以带子做到 22 单位（110→132），内轮盘 R1
由此定为 110。代价写在脸上：**轮心 182px → 140px，三粒骰跟着收到 34px**。跑道路径**在中线上按
弧长参数化**（`railPoint` 给坐标与朝外法线，9 段：4 直 4 弧、底边被起点切成两半），沿 t 采样 7 点
连折线——给外/内边界各自按比例分参会让角上的格子歪成梯形（两条边界周长不等）；折线误差最坏
0.056 单位 = 板宽 332 时的 0.058px。
⑰ **板抬头是一条钉死的工具带**——名牌与三枚圆钮都绝对定位在 stage 顶，stage 上沿由 HUD 决定、
HUD 是 `flex:none` 定高条，所以抽屉拉到任何档位它们一个像素都不动、也不跟着名牌被 `compact`
隐藏（🏦 正是现金不够时要找的那一个）。三条配套：圆钮改**横排**（竖排 126px 高，得把板推下去
126px 才不压格子）、`.board-wrap` **每一档都留** 46px、`.board-stage { min-height: 46px }`。
那个「每一档」是被自己的断言逮出来的：一度只在 peek 档留，理由是「half/full 档板窄、左右各空
60/100px」——**那是按一枚 46px 的钮算的**，横排之后那一列 126px 宽，居中的板横向根本让不开。
stage 同时改 `align-items: flex-start`：板贴着抬头排，否则屏一高两者之间空出两百像素。
⑱ **玩家主题色升为唯一权威**（新建 `web/src/playercolor.ts`，6 组 solid/soft/ink）——从前色相
公式在两个文件里各写一遍、饱和度明度按用途各写各的，头像圈还压根不参与（一律主色绿，六个人长得
一模一样）。两条硬约束写进模块头注释：**玩家色只上「人和人的道具」，永不上数字或状态**；
**颜色永远不是唯一线索**（一律带首字或形状）。`colorVars()` 让共用的类保持「没传玩家时退回主色」，
所以 `LobbyView` 那枚 chip 不用改——大厅还没有房间也就没有座位。
⑲ **奶酪与金币两种占位道具**。为此把 `ft_sold_squares` 从 `list[str]` 升成
`dict[square_id → player_id]` 并新增 `ft_claimed_dreams`。初稿本来要给「已买断但查不到主人」
画一枚**空心币**（一次性收益型企业 `ft-b-software`/`ft-b-biotech` 的 `cashflow` 为 0，进不了
`businesses`），房主问「游戏中真有这种情况吗」——查下来确实有，但**正确的修法是让它消失**：
每个卖掉的格子直接带着买主，连「遍历全员 businesses 反查」那一步也一起没有了。
深度分层：外 5 单位归色条，有道具时图标缩到 82% 贴色条、**道具贴内边界**（视线从盘心往外扫，
先看到「这块地有主了」）。加价角标 `×2` 走古铜、不上任何人的颜色——**价格不是归属**。
⑳ **线下辅助模式被波及一处**：`PlayerTableRow` 的 `.avatar-lg` 是两模式共用组件，线下牌桌那一行
的头像圈从「全是绿圈」变成「每人一色」。纯信息增益，不改任何点击路径。
测试 **530 passed**（基线 527 + 3 条新增，含「撤销重放后归属还在」；**已做负向对照**：删掉 apply
里那两行，三条当场挂）；`vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过 **74 屏**，新增 27e
「圆钮三档钉死」（三档 `top` 必须相等 + 抽屉高必须真的是三个值，否则「都一样」什么也没证明）
与 22 屏上的六条棋盘断言（24+48 格、焦点跟着我走、板圆角是圆角方、快车道 7 个字 41 枚图标、
一格 > 20px、奶酪数 = 玩家数、同一玩家棋子与座次点同色而不同玩家不同色）。
**负向对照做过**：把 `ftSquares` 改成 `[]`，22 屏那五条当场挂；把 `.board-wrap` 的留白退回
`:not(.compact)`，27e 三档全报「圆钮压在格子上了」。
**只差真机那一遍**：两条赛道同屏（需要一个人在快车道、一个人还在老鼠赛跑——纯线上没有确定路径
造这个局面，`ui-smoke` 暂时覆盖不到，见 design/09 §3 的遗留项）、23.5px 的格子在真机上读不读得出、
金币里 5.2px 的首字看不看得清。

**真机试玩带回两条（2026-08-13）**，纯呈现层，**引擎/协议/事件流/卡库零改动**：
㉑ **棋盘底部被裁**——`.board-stage`（`flex:1`+`overflow:hidden`）只拿 HUD 和抽屉分完剩下的
高度，不够就从底边裁；而 `boardWidth`（`OnlineRoomView.vue`）此前是写死的三档常量
（peek 332 / half 230 / full 150px），peek 档需要 `46（BoardView 根元素给名牌+圆钮留的
padding-top）+ 332 = 378px` 出头，只是按"典型 HUD 高度"估的数——HUD 一变高（资产条展开、
状态徽章变多）或设备可视高度更矮就会超支，超出的一截被咬掉。改成量出来再定宽：
`.board-stage` 挂 `ResizeObserver` 实测可用高度，`boardWidth` 按 `实测高度 − 46` 与原常量
取较小值（下限 120px），常量从此只是"够用时"的上限，不再是唯一依据。
㉒ **骰子还在转，「结束回合」已经可点**——`blockedBy`（判断按钮是否禁用）只看
`landing`/`activeCard` 是否 `resolved`，没管 `held`（动画还在播）。服务端一次性把整批事件
算完才广播，所以对**落点本身不需要决策**的格子（结算日、非强制市场风云卡等），
`landing.resolved` 在动画播放的同时就已经是 `true`，按钮当场变绿，与状态条上还在显示的
`heldTip`（"骰子还在转…"）自相矛盾；需要决策的格子（小生意等）则是巧合地因为
`resolved:false` 而正确地按住，两条格子背后其实是没对齐的两套判断。改法是 `held` 优先于
其它判断，动画播放期间**不管落点是哪种格子**一律禁用，文案直接复用 `heldTip`
（按钮上的字与状态条上的字永远是同一句话，不新造第二套措辞）。
测试 **530 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过
（74 屏，22 屏加了「棋盘底边不许探出 stage」的几何断言，24 屏加了「动画播放期间结束回合必须
disabled」的断言）。

**发牌/翻牌动画补一帧「结算」（2026-08-13，design/09 **v0.16**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**。用户反馈上一轮（v0.12）修完几何跳变后，牌堆卡飞出来「有点卡」、
职业卡「还是感觉会突变一下再翻面」。根因不是几何数值（`--fx/--fy/--fs` 仍是对的），是**时机**：
`DealCurtain.vue` 里内容到齐（`ready()` 从 false 变 true）与「起播 `.flying`/`.flipping` 两个
动画 class」挤在同一帧——牌堆卡这支还伴随 `fetchCards` 不带缓存触发的一轮偏重响应式重渲染
（`activeCardInfo` 是好几个下游 computed 的源头），浏览器要在同一帧里既排新内容的版又要起播
transform 动画，读起来是卡顿；职业卡内容虽轻，动画头几帧照样被这次绘制吃掉，读起来是先跳一下
再翻。修法是标准的「双 `requestAnimationFrame`」：新增 `animate` 状态位，内容到齐后先让它自然
挂载画一帧（仍是飞入前的静止姿态），下一帧才把 `animate` 置真、动画 class 真正生效——两次绘制
之间必然隔着至少一次真实绘制。`<slot>` 内容何时替换、`ready()` 何时求值这两条 v1.0/v0.12 定下的
铁律不动。测试 **532 passed**（后端未动，含并入 main 的其他改动，作回归）；`npx vue-tsc --noEmit` 通过；
`npm run ui-smoke` 反复跑多轮（10 次）全过（含 20c/20d/20e 职业卡揭牌三条与 26d 牌堆卡飞入位移那条，逐帧几何/连续性断言
均未受影响）。**只差真机那一遍**：这一轮改的是动画起播时机，开发机 Chromium 下能验证几何与
连续性不变，但「卡不卡」本身是手感判断，仍要等真机确认。

**抽卡翻牌动画「卡卡的，突然一下」的真正病灶已修（2026-08-13，design/09 **v0.17**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**。用户反馈 v0.16 之后真机上依然不丝滑——这次直接读代码找到病根，
且是代码自己的注释早就点名过、但从未真正修掉的问题（`DealCurtain.vue:28-31,108-118`、
`style.css:1319-1320` 里都写着"`fetchCards` 不带缓存"）：`fetchCards()`（`store.ts`）每次抽卡
都重新打一次网络请求要卡面数据，而 `DealCurtain` 的起播门槛 `ready()` 卡在这份数据到位——数据没
到，双 rAF 起播（v0.16 那道闸门）根本不会被调用，卡片就静止晾在飞入前那一帧，直到网络请求返回、
`onUpdated` 第一次触发起播，动画才真正开始播。开发机/本地回环网络下这次往返接近零延迟，几乎每次
都在同一帧内完成，所以前 7 轮在 `ui-smoke` 上全部验证通过；真机上哪怕是局域网也有几十到几百毫秒
延迟，于是卡片先晾着、数据一到"啪"地直接跳进整段已经调对的动画——这正是"卡卡的，突然一下"的
体感来源，而且是纯 CSS/时间轴层面完全调不出来的病灶：**问题不在动画本身，在动画获准开始播的那一刻
被网络请求推迟且推迟量不确定**。194 张卡在服务器启动时从 `server/data/` 一次性加载进内存、整个
会话期间不变，客户端整局缓存没有正确性风险。改法：`store.ts` 加 `cardsCatalog` 整局内存缓存，
`fetchCards()` 只有搜索（`q` 非空，`CardPicker` 用，服务端按 `ocr_keywords` 匹配、客户端没这份
字段没法等价复现）才真正打网络，其余按 `deck` 从缓存过滤；`OnlineRoomView.vue` 进房间时预取一次
（`onMounted` 里 `fetchBoard()` 旁边加 `fetchCards()`）。`DealCurtain.vue`/CSS 关键帧/`stage.ts`
的 `BEAT` 时间表一行未动——这些已经调对了。`CardPicker`/`ActionTab`/`PromptModal`/`RoomView` 四处
职业卡/求购卡列表调用同一个 `fetchCards`，全部只读不改数组，缓存对它们透明安全，顺带受益。
测试 **532 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过
（74 屏）。**只差真机那一遍**：这次要确认的不是几何/时间轴（那些已经反复验证过），是"进房间后第
一次抽卡"（预取可能还没完成）与"连续抽好几张"是否还会有静止一下再突然跳进动画的感觉。

**职业卡揭牌不再飞入，改成原地翻转（2026-08-13，design/09 **v0.18**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**。用户真机反馈：点职业卡牌背进入全屏帘幕后，翻转开始前
卡片大小依然会突变一下，观感不协调；用户明确表示宁可放弃「从点击的位置飞过来」这个花哨
效果，也要保证大小全程不跳变。这是同一个症状的第 6 轮修复——v0.12–v0.17 四轮都在修
「测量页内 `.prof-back` 的矩形 → 写入 `--fx/--fy/--fs` 三个 CSS 变量 → 双 `requestAnimationFrame`
等两帧才起播」这条链路本身的边角，每轮都在开发机 + `ui-smoke` 上验证通过，却从未在真机上
确认过；根因不是某次测量算错，是这条运行时矩形测量链路在真实设备的帧时序上不够稳，
且 `.prof-back`（父容器 `.page` 有 `max-width`）与 `.deal-card`（父容器 `.curtain` 铺满
视口）本来就不是同一个渲染宽度，差值虽小但在真机上比在开发机截图里更容易被人眼捕捉到。
这一轮索性砍掉飞入这一拍：`DealCurtain.vue` 的 `variant="reveal"` 分支不再做任何矩形测量、
不再计算 `fly`，`.deal-card.reveal` 直接 `transform: none`，从第一帧起就是它自己样式表
写死的终态尺寸（76%/max 300px），不存在任何依赖运行时测量的中间态；`.deal-inner.reveal.flipping`
的 `.28s` 起播延迟（原本用来等飞入先播完）一并去掉，翻转直接从第一帧起播，整段揭牌时长因此
从 2.2s 缩到 1.9s（`RoomView.vue` 的收尾 `setTimeout`）。代价是职业卡不再「从点击的位置飞过来」，
直接原地淡入 + 翻转——用户本人已认可这个取舍。`from`/矩形测量只留给 `deal` 变体（牌堆抽卡，
从棋盘格子飞出来，这条路径未受影响，也不在本次反馈范围内）。`ui-smoke` 的 20e 断言相应改写：
不再验证「第一帧压在被点的那张牌背上」，改为验证「帘幕挂载时 `.deal-card` 不挂任何动画、
`transform` 恒为 `none`、矩形宽度等于不带缩放的 `offsetWidth`」——做过负向对照：把
`.deal-card.reveal { transform: none }` 那条 CSS 规则去掉，20e 立刻报「还在套用飞入/缩放
（`matrix(0.55, 0, 0, 0.55, 0, 0)`）」。
测试 **532 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过
（74 屏，20e 已改写为新口径并验证过负向对照）。**只差真机那一遍**：这次去掉的是一整条运行时
测量链路，理论上不该再有真机专属的时序问题，但仍需用户在真机上确认翻转前的突变感是否真的消失。

**掷骰界面：轮心减负 + 选粒数入抽屉 + 抽屉压紧（2026-08-13，design/09 **v0.19**）**，
纯呈现层，**引擎/协议/事件流/卡库零改动**。真机试玩反馈：掷骰前抽屉状态条与按钮之间空出
一大截；棋盘收窄到 compact 档时轮心里「选几粒骰子」的药丸按钮（慈善生效才出现）不跟着
`--bw` 缩放，被圆形轮心边界挤/裁变形。改法是三处一起动：① 轮心（`#hub` 插槽）撤掉
`.dice-pick` 与状态文字 `.hub-tip`，只留骰盘本身——`hubTip` 的每条分支（连不上/别人回合/
停赛/慈善/第几步）在抽屉状态条 `.drawer-peek` 里都已经各有一份，去掉不丢信息，`hubTip`
这个 computed 随之整个删除。② 选粒数选择器搬进 `.drawer-cta`，紧贴在「掷 N 粒骰」按钮
正上方，和它同挂一条 `v-if`/`v-else-if` 链（`cardCta` → 选择器 → 掷骰行），条件严格是
掷骰行的子集（多了 `diceMax > 1`），两者必然同开同关；搬进抽屉后天然脱离 `--bw` 缩放
上下文，`.dice-pick` 从「轮心里挤出来的小药丸」改成 `flex:1` 铺满整行，不需要任何缩放算式，
变形问题随位置一起消失。③ 抽屉「压不下去」的根因不是文字多少，是 `.drawer-cta` 的
`position:sticky;bottom:0`——`.board-drawer` 没设 `overflow`，sticky 的包含块一路逃逸到
`.board-page`，抽屉一矮按钮区就整块贴死在屏幕真实底边、盖住状态条（`DETENT_H.peek`/
`FLOOR_H`/`detentPx()` 曾经被迫钉成同一个 180，就是在合力掩盖这个 bug，真机复现过）。
删掉 `position:sticky` 换成 `relative`（`.drawer-cta::before` 那层渐变遮罩仍有 positioned
祖先），抽屉从此是普通 flex-column 里排最后的兄弟节点，不会再逃逸。sticky 修掉之后
`FLOOR_H`（内容自适应的下限）与 `DETENT_H.peek`/`detentPx()`（内容多时的上限、手动拖拽
下限）不再需要绑成同一个数字，`FLOOR_H` 从 180 降到 96——一个跟内容形状无关的通用最小值，
够露出把手 + 状态条一行；`DETENT_H.peek` 维持 180px 不变（它管的是长昵称换行等最坏情形
的上限，不是这次要解决的问题）。落地前先用静态 HTML 效果图（改前/改后对比，含普通掷骰与
慈善生效两种场景）经用户确认版式，再照稿子改代码。
测试 **532 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke`
全过（74 屏，含跟改动前逐屏对照过一次确认无新增溢出警告）。**只差真机那一遍**：抽屉不空了、
选择器不变形、状态条没被按钮盖住；慈善生效时选粒数在抽屉里的手感也要真机确认。

**抽屉「装不下」与棋盘「一直是缩小版」（2026-08-13，design/09 **v0.20**）**，纯呈现层，
**引擎/协议/事件流/卡库零改动，线下辅助模式一屏一字未变**（逐屏比对父提交 5bf8b3b：线下 41 屏里
`03*`–`15` 一整组字节一致，其余差异全部来自随机职业与梦想 / 日志时间戳）。用户带回两张真机截图：
「上一轮修改的，抽卡按钮显示不完整并没有完全改好」「明明有空间，但是这个棋盘显示缩小版，没有文字的」。
㉓ **抽屉的内容高度改成量「壳 + 内容」，不再把几个孩子加起来。** 同一个症状这是第三次修：先漏了
三个被测元素各自的 padding（`contentRect` 是内容盒），上一轮改用 `borderBoxSize` 之后**又漏了
把手那 4px + 8px 外边距与抽屉自己 1px 的上边框**，合计 13px——机会格那一屏「小生意 / 大买卖」
在正文里（不在 `.drawer-cta`），13px 全落在它们身上，按钮下半截被 `.drawer-body` 裁掉。
根子不是哪一次算错，是**那份加数清单只要抽屉里再多一个兄弟节点就会过期一次**。
通则：**要量一堆东西的总和，就去量那个总和，别去量它的加数。** `.drawer-body` 是抽屉里唯一
`flex: 1` 的孩子（其余一律 `flex: none`，为此把 `.drawer-peek` 也钉死），所以
`抽屉高 − 正文高` 恒等于「壳」，把手/状态条/按钮区/边框/安全区一个不漏；正文被压到 0 的那一帧
量不出壳，留上一次的值（**绝不能让抽屉越算越矮**）。
㉔ **棋盘尺寸与「格面写不写字」都改由实测可用空间决定。** §2.2 的表本来就写着「等比缩小到
**装得下**」，v0.15 却把它实现成两档常量（peek 332 / half·full 230）——**档位说的是「抽屉想要
多高」，不是「棋盘还剩多少地方」**，而 v0.19 抽屉贴合内容高度之后这两件事彻底脱钩：停在机会格时
抽屉只有一百多像素，stage 还剩三百多，棋盘却照旧被按在 230 上，格面文字与名牌一并退场。
现在只留上限 332，宽度 = `min(332, stage 实测净宽, stage 实测净高 − 46)`；`compact`
（格面文字与名牌退场）改成**板宽的结果**——格面文字是 SVG 单位 9/320，板宽 300 时才 8.4px，
所以判据是 `< 300px`，与档位无关。**横向也按实测收**：`max-width:100%` 只挡得住溢出，
挡不住「板其实只有 288 宽却按 332 判定成写得下字」。
㉕ 顺带两条：① **一行都排不出来时 `.drawer-cta` 整块不渲染**（空着渲染就是抽屉底下一条谁也
点不着的死白，而抽屉压紧之后这条死白是从棋盘那儿夺来的），安全区随之从它身上移到**抽屉自己**
——不归里面某一个可能消失的孩子；② v0.19 把**慈善粒数选择器与掷骰按钮写成了同一条 `v-else-if`
链上的两支**，条件又恰好是子集关系，于是慈善一生效掷骰按钮就被选择器顶掉了（只能去点轮心那颗
骰子），改成同一支里的两行。
测试 **532 passed**（后端未动，作回归）；`npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过（74 屏）。
25 屏新增 5 条断言：正文不许有滚动条 + 抽牌按钮整行不许被切 + `.drawer-cta` 不许空着在场 +
板宽 ≥300 + 内圈格面文字 ≥12 且名牌在场。**负向对照做过**：把量法退回「三个孩子相加」当场报
「正文还差 13px 才装得下 / 抽牌按钮被切掉 5px」，把板宽退回两档常量当场报「stage 底下还空着
249px，棋盘却只有 230px / 内圈只有 0 个格面文字 / 没有名牌」。
**只差真机那一遍**：机会格那一屏两个抽牌按钮整个露出来、棋盘长回写字版；慈善生效时抽屉里
「1 粒 / 2 粒」与「掷 N 粒骰」同时在场（ui-smoke 造不出慈善这一局面，掷骰点数由服务端定）。

**格面文字的隐藏阈值从 300 调到 240（2026-08-14，design/09 v0.21）**，纯呈现层，
**引擎/协议/事件流/卡库零改动**。用户带回两张真机截图：手机屏幕较小/较窄时，棋盘缩到
`compact` 档、格面文字与名牌整体隐藏，但抽屉上方明显还留着没用完的空间。根因不是测量时序
bug——`boardPx = min(332, stage 实测净宽, stage 实测净高 − 46)`，棋盘是正方形，竖屏下
往往是**宽度**先顶到 stage 边界收窄（`.board-stage` 水平内边距只有 24px，但窄屏/分屏设备
仍能把可用宽度压到 300 以下），这时棋盘按宽度定形，纵向自然空出一截没用完的 stage 高度——
这是几何的必然结果，不是 bug；`BOARD_TEXT_MIN=300` 这道阈值本身卡在了这条路径上。
`design/09` §2.2 写的判据依据「板宽 300px 时格面文字渲染出来才 8.4px，再窄就只剩色块」，
是当初「接近能看清的下限」的保守估计，不是精确算出来的极限值。用真实两字格名（结算/市场/
支出/慈善/孩子/失业/税审）在 3x DPR 下逐档截图比对：240（≈6.75px）依然清楚可辨，
200（≈5.6px）复杂字形才开始发糊——改成 240（`web/src/views/OnlineRoomView.vue` 的
`BOARD_TEXT_MIN`），留了余量，比旧阈值更倾向于显示文字。`BOARD_MAX`/`BOARD_MIN`/
`BOARD_HEAD_H` 三个联动常量未动（46 是圆钮带的固定占用，不随 compact 变化，这条链路本身
没问题，只改了「多窄算窄」这道判据）。
测试 `npx vue-tsc --noEmit` 通过；`npm run ui-smoke` 全过。25 屏新增一段窄视口断言
（`web/scripts/ui-smoke.mjs`）：把 `pMe` 的视口临时收到 294px 宽，把板宽逼到新旧阈值之间
（240~300），验证这时候文字与名牌仍然在场，断言完再把视口还原；**负向对照**：把
`BOARD_TEXT_MIN` 改回 300，这一段当场报「板宽够却没写字」。这段断言只在这一局恰好走到
机会格时触发（掷骰点数由服务端定，与既有 25 屏其余断言同一条件）。
**只差真机那一遍**：240px 板宽下两字格名在真实设备上是否还看得清、之前反馈的「抽屉上方空一截」
的具体场景这次是否已经不再触发 compact。

## 开发必读（按顺序）

1. [design/09-纯线上版UX设计.md](design/09-纯线上版UX设计.md) — 纯线上模式的信息架构、棋盘、回合流、动效节拍表（呈现层权威）
2. [design/08-浏览器端OCR方案.md](design/08-浏览器端OCR方案.md) — 浏览器端 OCR 的实现、实测数据、剩余验收项
3. [design/06-卡牌数字化结果与设计修订.md](design/06-卡牌数字化结果与设计修订.md) — 194 张卡的字段设计定稿、17 个 subtype、已定案的裁决点
4. [design/02-游戏规则引擎规格.md](design/02-游戏规则引擎规格.md) — **规则的唯一权威**：公式、状态机、卡牌效果、测试要求。不确定处回查说明书（`python tools/render_manual.py` 渲染成图）
5. [design/00-文档索引.md](design/00-文档索引.md) — 全部文档索引与关键决策速览
6. [design/03-系统架构与详细设计.md](design/03-系统架构与详细设计.md) — 技术栈、数据模型、WS/REST 协议、事件溯源、部署
7. [design/01-需求规格说明书.md](design/01-需求规格说明书.md) — 功能需求（FR-1~31）、里程碑、决策记录
8. [design/05-棋盘格子数据.md](design/05-棋盘格子数据.md) — 内圈序列 + 快车道全部格子数值（已核实）
9. [design/07-v3卡库实现交接.md](design/07-v3卡库实现交接.md) — v3 卡库升级的不兼容清单与实施顺序（已全部完成，历史参考）

（[design/04](design/04-卡牌数据模型与识别方案.md) 的卡牌部分已被 06 取代，只有识别管线一节仍有效。）

`docs/` 是原始资料（说明书扫描件、记录卡、194 张卡片实拍、棋盘照片），只读不改。

## 关键决策（已与用户确认，勿再讨论）

- **技术栈**：后端 Python 3.12 + FastAPI + SQLite（事件溯源，规则引擎为纯函数模块）；前端 Vue3 + Vite + TS + Pinia（PWA）；交付 Docker 镜像 + compose（房主 Windows 11 笔记本运行）
- **数据权威**：卡牌/格子数据 = `server/data/` 下的 JSON 文件（人工可改，进 git）；SQLite 只是启动时导入的缓存
- **卡库双轨**：`raw` 存卡面原文逐字转录（纯线上版据此渲染卡面，不得省略）+ `data` 存结构化数值（引擎唯一取数来源）
- **重复卡是真实牌堆构成**：实体牌堆有 9 组共 20 张完全相同的卡，直接决定抽牌概率，**必须按实际张数入库**，靠 `key` 归组、`duplicateOf` 标注
- **字段设计三原则**：能推出的不存（如 `effects` 由 subtype 推）；一个字段只有一种语义；通用规则不逐卡标注（如银行贷款是全局规则）
- **识别**：封闭集匹配（OCR 认出"是哪张卡"，数值取库）；OCR 跑在手机浏览器上（服务端零内存开销），服务端 PaddleOCR 退为局域网可选降级档，云端 OCR API 只留接口不实现；手动选卡是永远可用的兜底
- **无实体现金**：App 即银行+记录卡，玩家现金显示在"银行储蓄"栏
- **一切按说明书规则**（含进快车道时现金交回银行）；App 内要有说明书查看入口（/manual）
- **开发顺序**：M0 录入工具与数据文件 → M1 无 OCR 可完整玩一局老鼠赛跑 → M2 快车道/破产/交易 → M3 OCR → M4 云部署

## 硬性质量门槛

- 规则引擎必须先有 design/02 §13 的全量测试（含说明书示例数值回归：医生/经理/P.12 三组）再算完成
- 所有账目由服务端计算，客户端不做权威计算；每个行动是不可变事件，状态 = reduce(初始, 事件流)
- 改完卡库必跑 `python tools/validate_cards.py`（schema + 恒等式 + 张数对账 + 资产↔求购卡交叉检查）
- **`MARKET_SELL_OFFER` 只推要约、绝不自动成交，也不得因"卖了会亏"而过滤或隐藏要约**——玩家可能为套现渡过破产危机而主动贱卖

## 易犯的错：把卡面叙述当成游戏机制

数字化过程中已犯过三次同类错误（详见 design/07 §7）。看到卡面某句话想加字段时先问：**这是规则，还是氛围？**

- 「目前没有其他买主」→ 剧情，不是"不可转售"
- 「可以通过条款取得银行贷款」→ 说明书 p6 的全局规则，不是该卡特权
- 「卖了会亏」→ 出售始终可选，不构成匹配矛盾

`raw` 已完整保存原文，`data` 里塞不必要的字段只会让引擎多出错误分支。
`tools/validate_cards.py` 有一份**已删字段黑名单**，加回去会直接报错。

## 环境备注

- 开发机 Windows 11，shell 是 PowerShell（5.1，无 `&&`）；根目录用系统 Python 跑 `tools/*.py`，**server 必须用 `server\.venv\Scripts\python.exe`**（项目锁 3.12，PaddleOCR 兼容性）。`tools/eval_browser_ocr.py` 要 import server 的模块，也得用 venv 的 python
- `web/` 下的 npm 脚本：`ocr-bench`（194 张实拍图跑 OCR）、`ocr-smoke`（识别链路真实浏览器冒烟）、`sync-tesseract`（构建期自动跑，一般不用手动）、`ui-smoke`（**UI 冒烟**：真浏览器开两个上下文跑一局，从房间准备一路走到快车道，逐屏截图并断言关键选择器出现，产物在 `build/ui-smoke/`）。三个浏览器相关的脚本都用系统装的 Edge，先 `npm run build`
- 前端类型检查：`npx vue-tsc --noEmit`（vite build 本身不做类型检查）
- 设计文档与代码注释、UI 文案一律中文
- `build/` 是工具产物（裁剪图、核对页、说明书渲染图），不进 git，可随时重新生成
- 说明书两条渲染链路别混：`tools/render_manual.py` → `build/manual/*.png`（74MB，给人/模型核对规则用）；`tools/build_manual_pages.py` → `server/manual_pages/p01..p16.webp`（3.5MB，App 的 /manual 页面用，**入 git 随镜像交付**）。两者都从 `docs/现金流游戏说明书.pdf` 生成
