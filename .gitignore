# Python
__pycache__/
*.pyc
server/.venv/
.pytest_cache/
.hypothesis/

# 运行时数据（事件日志数据库，勿入库）
server/cashflow.db*
*.db-wal
*.db-shm
server/uvicorn.*.log

# 自签 HTTPS 证书（含私钥，启动时由 app/certs.py 自动生成，勿入库）
server/certs/

# 前端
web/node_modules/
web/dist/
# tesseract.js 运行时资源（~12MB，构建期由 scripts/sync-tesseract-assets.mjs 从
# node_modules 拷进来，prebuild 钩子自动跑，不进 git）
web/public/tesseract/


# 卡片原始高清照片（313MB，仅本地保留，仓库里放 docs/实体卡片/ 的压缩版）
docs/实体卡片_原图/

# 工具产物（134MB）：卡片裁剪图、人工核对页、说明书渲染图
# 全部可由 tools/crop_cards.py、build_review.py、render_manual.py 重新生成
build/
